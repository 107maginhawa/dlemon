---
context: default
phase: enforce-fix (run-7 P0 cleanup, post-Wave4)
task: cleanup-wave in progress (uncommitted)
total_tasks: null
status: in_progress
last_updated: 2026-05-29T04:58:25.287Z
head_sha: 267e7311
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure this session.

- [ ] CONSTRAINT: **Gates not yet run on the uncommitted wave** — the ~62 modified files (EF-PAT-004 branch-access + EF-PMD-005 schema/checksum) have NOT been typechecked or tested. Wave4 report flagged 3 live prod typecheck errors in `dental-pmd/importPMD.ts`. Run codegen → typecheck → test BEFORE committing. Do not assume green.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Commit-before-verify | Wave4 left 3 prod typecheck errors; this wave fixes them but hasn't re-run gates | blocking | Run `bun run typecheck` + `bun test` and read output before any commit |
| Committing agent worktrees | `.claude/worktrees/agent-*` dirs are ephemeral scratch from parallel agents | advisory | Never `git add` them; `git status --short \| grep '^??'` should show only worktrees, ignore them |
| dental-pmd.tsp edited without regen | `.tsp` changed (sourceDescription, checksum) but OpenAPI/types not regenerated | blocking | `cd specs/api && bun run build` then `cd services/api-ts && bun run generate` before typecheck |

<current_state>
Brownfield enforcement stabilization (NOT a gsd phase — no `.planning/phases/`).

- **Run-7 enforcement** just ran (oli-enforce-all --strict): 346 findings, 54 P0, 142 P1, 8 new P0 regressions, verdict FAIL. Audit docs (`docs/audits/ENFORCEMENT_REPORT.md`, `enforce/**`, `.baseline.json`, `trace.md`) are written but **uncommitted**.
- **Wave 4 enforce-fix is committed** at HEAD `267e7311` (40 P0s: 22 mechanical + 18 structural; 4 BLOCKED). Report: `docs/audits/ENFORCEMENT_FIX_REPORT.md`.
- **Uncommitted cleanup wave in progress** — the direct follow-on to Wave4 Next-Step #1:
  - dental-pmd `EF-PMD-005`: added `sourceDescription` (required) + `checksum` (optional sha256, server-verified, mismatch→422) to `dental-pmd.tsp`, `repos/pmd-document.schema.ts`, `importPMD.ts`, and tests.
  - dental-patient `EF-PAT-004`: `assertBranchAccess(db, user.id, patient.preferredBranchId)` inserted across ~27 handlers (alerts, contacts, engagement, identity, insurance, recalls, sync, treatment-plans).
  - dental-audit: 1-line `repos/audit-log.schema.ts` tweak.
</current_state>

<completed_work>
- Run-7 enforcement executed (docs written, uncommitted).
- Wave 4 enforce-fix committed (267e7311).
- Cleanup-wave edits applied to handlers/schema/tsp/tests (uncommitted, unverified).
</completed_work>

<remaining_work>
1. Regen spec/types: `cd specs/api && bun run build` → `cd services/api-ts && bun run generate` (dental-pmd.tsp changed).
2. `bun run typecheck` — confirm 3 `dental-pmd/importPMD.ts` prod errors resolved, no new errors from EF-PAT-004 edits.
3. `bun test` (dental-pmd, dental-patient, acceptance) — no regressions.
4. Commit cleanup wave (split: dental-pmd schema/checksum · dental-patient EF-PAT-004 · dental-audit schema).
5. Commit run-7 audit docs.
6. Remaining run-7 P0s not in Wave4 scope: `EF-ORG-P015` (cross-branch PIN reset — add assertBranchAccess in `pinRecovery.ts`), `EM-PAT-001/002/003` (use `assertBranchRole(['dentist_owner'])` not `assertBranchAccess` on export/bulk-archive/restore).
7. Systemic: `DE-001..DE-023` domain events 0% wired — blocked pending F2.
</remaining_work>

<decisions_made>
- dental-pmd ImportPMDRequest: `sourceDescription` required, `checksum` optional (sha256-<hex>, verified server-side, mismatch→422) — EF-PMD-005 provenance + fixes Wave4's 3 typecheck errors.
- Branch auth guarded on `patient.preferredBranchId` presence before calling `assertBranchAccess` — EF-PAT-004.
</decisions_made>

<blockers>
- DE-001..DE-023 domain events dead (event bus unwired) → blocked pending F2 service-layer/DI sprint.
- EF-EMR-001 / EM-EMR-001-003: emr/ rename needs spec-phase decision.
- EM-VIS-011: async visit-lock expiry needs pg-boss (not provisioned).
</blockers>

## Required Reading (in order)
1. `docs/audits/ENFORCEMENT_FIX_REPORT.md` — Wave4 summary + Next Steps (this wave = Next-Step #1).
2. `docs/audits/ENFORCEMENT_REPORT.md` (run-7) — full 346-finding inventory, P0 list, module dashboard.
3. `.planning/STATE.md` — structural-remediation milestone (Phases 0-12 complete) + F1-F7 future work.

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: committing before gates → run typecheck + test first.
- [ANTI-PATTERN]: regenerated files skipped after .tsp edit → run codegen before typecheck.

## Infrastructure State
- No dev servers running. HEAD = 267e7311 on `main`. Working tree dirty (62 files + ephemeral worktrees).

<context>
Mid-cleanup-wave following the Wave4 enforce-fix sprint. The mental model: Wave4 fixed 40 P0s but explicitly deferred 3 prod typecheck errors and the EF-PAT-004 branch-auth gap to a follow-on; that follow-on is exactly the uncommitted diff. Resume by verifying then committing — do not start new findings until this wave is green and committed.
</context>

<next_action>
Start with: `cd specs/api && bun run build && cd ../../services/api-ts && bun run generate && bun run typecheck` — confirm the dental-pmd/importPMD.ts errors are gone, then run `bun test` before committing.
</next_action>
