---
context: phase
phase: G1-foundation-stabilization
task: P2-001
total_tasks: 6
status: in_progress
last_updated: 2026-05-25T12:48:51.128Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| mock.module placement | sonner mock must be declared at module top-level (before imports) — Bun hoists it correctly when top-level | advisory | Place mock.module call immediately after bun:test import, before any other imports |

<current_state>
Branch: feat/v1.5-g1-foundation

All P1 items are DONE:
- P1-004 ✅ (82ec9e6) — CI exit gate hardened
- P1-001 ✅ (59d8abc) — BR-011 consent gate
- P1-002 ✅ (1e61bd5) — dental_chart_version audit table + saveVersion() + migration 0040
- P1-003 ✅ (ef82e2c) — onError toasts: useSaveChart (VISIT_LOCKED-aware), useSaveTreatment, useUpdateTreatment

Next task is **P2-001**: write dental-perio handler coverage tests (not started).

After P2-001: audit rebaseline (mark GAP-DENTAL-001..004 RESOLVED).
</current_state>

<completed_work>

- P1-002: dental_chart_version schema (versionedSnapshotFields + chartId FK + unique(chartId,version)), DentalChartRepository.saveVersion() using createSnapshotVersion helper, called from upsertDentalChart after every upsert. Migration 0040. TDD_PROOF at docs/execution/slices/p1-002-chart-version-audit/TDD_PROOF.md.

- P1-003: onError toast added to useSaveChart (VISIT_LOCKED-specific message via JSON.stringify scan), useSaveTreatment, useUpdateTreatment. mock.module('sonner') pattern established for hook tests. TDD_PROOF at docs/execution/slices/p1-003-error-toast/TDD_PROOF.md.
</completed_work>

<remaining_work>

- **P2-001** (NOT STARTED): `services/api-ts/src/handlers/dental-perio/dental-perio-coverage.test.ts`
  - Cover: createPerioChart, upsertToothReading, completePerioChart, getVisitPerioChart + RBAC 403
  - Real DB, `pp` UUID prefix (e.g. `pp000000-0000-1000-8000-000000000001`)
  - Pattern: local Hono app + injected user/database context (see dental-visit-module3.test.ts:83 for buildTestApp pattern)
  - Seed: org, branch, membership (dentist_owner), person, patient, active visit — all pp-prefixed
  - Routes not yet in app.ts — test registers them inline
  - TDD: write tests first (RED), then confirm handlers pass (GREEN should be immediate since handlers exist)

- **AUDIT-REBASELINE** (after P2-001):
  - Mark GAP-DENTAL-001..004 RESOLVED in `docs/audits/DENTAL_GAP_REGISTRY.md`
  - Update `docs/audits/DENTAL_SYSTEM_AUDIT_REPORT.md`
</remaining_work>

<decisions_made>

- saveVersion called from handler (not inside repo.upsert) — keeps upsert atomic, version is a handler-level side-effect
- VISIT_LOCKED detection: `JSON.stringify(err).includes('VISIT_LOCKED')` — SDK error shape is opaque, stringify is most reliable
- P2-001 tests go in a new file `dental-perio-coverage.test.ts` (not the existing repo test)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `services/api-ts/src/handlers/dental-visit/dental-visit-module3.test.ts` lines 52–110 — buildTestApp pattern for inline Hono test app with user/db injection
2. `services/api-ts/src/handlers/dental-perio/createPerioChart.ts` — handler to test (note: uses membership lookup)
3. `services/api-ts/src/handlers/dental-perio/repos/perio-chart.repo.test.ts` — existing fixture setup using `seedClinicalChain`

## Infrastructure State
- PostgreSQL: running on localhost:5432/monobase
- Migration 0040 applied (dental_chart_version table exists)
- Branch: feat/v1.5-g1-foundation — all changes committed, clean tree

<context>
Sprint is in good shape. All P1 gaps are closed. P2-001 is the remaining coverage gap. The dental-perio handlers already exist and work — we just need the coverage test file. Session ended due to context exhaustion.
</context>

<next_action>
Start with: Create `services/api-ts/src/handlers/dental-perio/dental-perio-coverage.test.ts`. Follow dental-visit-module3.test.ts buildTestApp pattern. Use `pp` UUID prefix for all fixtures. Seed: org → branch → membership(dentist_owner, personId=TEST_USER.id) → person → patient → active visit. Register perio routes inline. Test: createPerioChart (201), upsertToothReading (200), completePerioChart (after seeding 16 readings), getVisitPerioChart (200 with readings), RBAC 403 for non-dentist.
</next_action>
