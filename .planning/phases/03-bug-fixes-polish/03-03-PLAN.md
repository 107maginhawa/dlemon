---
phase: 03-bug-fixes-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/dentalemon/src/features/workspace/hooks/use-save-treatment.ts
  - apps/dentalemon/src/features/workspace/components/treatment-table.tsx
  - apps/dentalemon/src/features/workspace/components/workspace-payment-modal.tsx
  - apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx
autonomous: true
requirements:
  - BFIX-01

must_haves:
  truths:
    - "Price saved to API is always Math.round(dollars * 100) cents, never Math.round(dollars)"
    - "Price displayed in treatment table is priceCents / 100"
    - "Price displayed in payment modal is priceCents / 100 via formatCents()"
    - "Price displayed in tooth slideout review step is parseFloat(priceInput).toLocaleString() where priceInput is dollars"
  artifacts:
    - path: "apps/dentalemon/src/features/workspace/hooks/use-save-treatment.ts"
      provides: "Correct cents conversion: Math.round(priceAmount * 100)"
      contains: "Math.round(priceAmount * 100)"
  key_links:
    - from: "CdtCodeBrowser"
      to: "ToothSlideout"
      via: "onSelect callback passing priceCents"
      pattern: "setPriceInput\\(String\\(selection.priceCents / 100\\)\\)"
    - from: "useSaveTreatment"
      to: "createDentalTreatment API"
      via: "priceCents body field"
      pattern: "Math\\.round\\(priceAmount \\* 100\\)"
---

<objective>
Fix the price ×100 boundary bug (BFIX-01): the save mutation sends dollars as cents, causing 100× undercharging. Audit all display and save sites to confirm the contract is enforced everywhere.

Purpose: Prices displayed and saved must be consistent — display reads cents and divides, save takes dollars and multiplies.
Output: Corrected use-save-treatment.ts + verified display sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bug-fixes-polish/03-CONTEXT.md
</context>

<interfaces>
<!-- Price flow through the system — executor must understand this end-to-end -->

Price contract:
- CDT code database stores: `defaultPriceCents` (integer, e.g. 150000 = $1500)
- CdtCodeBrowser → ToothSlideout: `selection.priceCents` (cents integer)
- ToothSlideout stores: `priceInput` as STRING of dollar amount (e.g. "1500.00")
  - Set via: `setPriceInput(String(selection.priceCents / 100))` ← correct ÷100
- ToothSlideoutData.priceInput: dollar string passed to useSaveToothFlow
- useSaveToothFlow sets: `priceAmount = parseFloat(data.priceInput)` ← dollar float
- useSaveTreatment receives: `priceAmount` as dollars (e.g. 1500.0)
- useSaveTreatment sends to API: `priceCents: BigInt(Math.round(priceAmount))` ← BUG: should be `Math.round(priceAmount * 100)`
- API stores: priceCents as integer
- Display reads: `item.priceCents / 100` ← correct

The single bug is in use-save-treatment.ts line 36.
Display sites (treatment-table.tsx, workspace-payment-modal.tsx, tooth-slideout.tsx review step) are all correct.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BFIX-01 — Fix cents conversion in use-save-treatment.ts</name>
  <files>
    apps/dentalemon/src/features/workspace/hooks/use-save-treatment.ts
  </files>
  <read_first>
    - apps/dentalemon/src/features/workspace/hooks/use-save-treatment.ts
  </read_first>
  <behavior>
    - Input: priceAmount=1500 (dollars)
    - Before fix: priceCents = BigInt(Math.round(1500)) = 1500n (= $15.00 — wrong)
    - After fix: priceCents = BigInt(Math.round(1500 * 100)) = 150000n (= $1500.00 — correct)
  </behavior>
  <action>
Read use-save-treatment.ts. Find line 36:
```typescript
priceCents: BigInt(Math.round(priceAmount)),
```

Replace with:
```typescript
priceCents: BigInt(Math.round(priceAmount * 100)),
```

This is the only change needed in this file. The `priceAmount` parameter already carries the dollar value (e.g. 1500.0) as confirmed by use-save-tooth-flow.ts line 63 (`priceAmount = raw` where `raw = parseFloat(data.priceInput)` and priceInput holds a dollar string like "1500").

Add a comment on the next line to document the contract:
```typescript
priceCents: BigInt(Math.round(priceAmount * 100)), // priceAmount is dollars; multiply by 100 for cents
```
  </action>
  <verify>
    <automated>grep -n "Math.round(priceAmount \* 100)" /Users/eladventures/Desktop/dentalemon/apps/dentalemon/src/features/workspace/hooks/use-save-treatment.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Math.round(priceAmount \* 100)" use-save-treatment.ts` returns exactly 1 result
    - `grep "Math.round(priceAmount)" use-save-treatment.ts | grep -v "\\* 100"` returns 0 results (old form gone)
    - Comment "priceAmount is dollars" present on that line
    - No TypeScript errors on the file
  </acceptance_criteria>
  <done>Save mutation sends correct cents value: $1500 → 150000 cents</done>
</task>

<task type="auto">
  <name>Task 2: BFIX-01 — Audit all price display sites</name>
  <files>
    apps/dentalemon/src/features/workspace/components/treatment-table.tsx,
    apps/dentalemon/src/features/workspace/components/workspace-payment-modal.tsx,
    apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx
  </files>
  <read_first>
    - apps/dentalemon/src/features/workspace/components/treatment-table.tsx
    - apps/dentalemon/src/features/workspace/components/workspace-payment-modal.tsx
    - apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx
  </read_first>
  <action>
Audit each file for price display. Confirm each site follows the contract. Fix any deviations found.

TREATMENT TABLE (treatment-table.tsx):
Locate all priceCents references. Expected patterns:
- Display: `item.priceCents / 100` ← correct
- Subtotal sum: `sum + (i.priceCents / 100)` ← correct  
- Inline edit save: `priceCents: cents` where `cents = Math.round(parsed * 100)` ← correct
If all match, no change needed. Add comment `// priceCents / 100 for display` if missing for clarity.

WORKSPACE PAYMENT MODAL (workspace-payment-modal.tsx):
Expected: `formatCents(item.priceCents)` — check that `formatCents` divides by 100 internally, or that priceCents is passed and displayed as cents/100.
Read the formatCents function definition in this file. If it does NOT divide by 100, add the division.

TOOTH SLIDEOUT (tooth-slideout.tsx):
Review step displays priceInput (dollar string), not priceCents. Expected:
```typescript
{CURRENCY_SYMBOL}{parseFloat(priceInput).toLocaleString(APP_LOCALE)}
```
This is correct — priceInput is already in dollars. No change needed.

CdtCodeBrowser sets priceInput via:
```typescript
setPriceInput(String(selection.priceCents / 100));
```
This is already correct (divides by 100 before storing as dollar string).

After audit, produce a brief comment in each file near the price calculation documenting the contract:
`// price contract: priceCents (API) ÷ 100 → dollars (display)`

Run `bun run typecheck` to confirm no regressions.
  </action>
  <verify>
    <automated>grep -n "priceCents / 100\|formatCents" /Users/eladventures/Desktop/dentalemon/apps/dentalemon/src/features/workspace/components/treatment-table.tsx</automated>
    <automated>grep -n "priceCents / 100\|formatCents" /Users/eladventures/Desktop/dentalemon/apps/dentalemon/src/features/workspace/components/workspace-payment-modal.tsx</automated>
    <automated>grep -n "price contract" /Users/eladventures/Desktop/dentalemon/apps/dentalemon/src/features/workspace/components/treatment-table.tsx /Users/eladventures/Desktop/dentalemon/apps/dentalemon/src/features/workspace/components/workspace-payment-modal.tsx /Users/eladventures/Desktop/dentalemon/apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx</automated>
    <automated>cd /Users/eladventures/Desktop/dentalemon && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - treatment-table.tsx: every price display uses `priceCents / 100`, every save uses `Math.round(parsed * 100)`
    - workspace-payment-modal.tsx: price display correctly shows dollars (either via `priceCents / 100` or formatCents that divides internally)
    - tooth-slideout.tsx review step displays `parseFloat(priceInput).toLocaleString()` where priceInput is a dollar string
    - "price contract" comment present in all 3 files
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All 3 price display sites verified correct; save boundary enforces ×100</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → price cents | User types dollar amount; must be validated as numeric before multiplication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | useSaveTreatment price calculation | mitigate | Fix ×100 multiplication — prevents undercharging by 100× which would affect billing integrity |
| T-03-03-02 | Information Disclosure | Price displayed to patient | mitigate | Ensure display uses priceCents / 100 — incorrect display could mislead patient about cost |
| T-03-03-03 | Tampering | CdtCodeBrowser priceInput NaN | accept | `isNaN(raw)` guard already present in cdt-code-browser.tsx:164 before onSelect fires |
</threat_model>

<verification>
```bash
cd /Users/eladventures/Desktop/dentalemon
# Confirm fix
grep -n "Math.round(priceAmount" apps/dentalemon/src/features/workspace/hooks/use-save-treatment.ts
# Confirm display sites
grep -rn "priceCents / 100" apps/dentalemon/src/features/workspace/components/treatment-table.tsx
grep -rn "formatCents\|priceCents" apps/dentalemon/src/features/workspace/components/workspace-payment-modal.tsx | head -10
# Full typecheck
bun run typecheck
```
</verification>

<success_criteria>
- use-save-treatment.ts: `Math.round(priceAmount * 100)` (was `Math.round(priceAmount)`)
- treatment-table.tsx, workspace-payment-modal.tsx, tooth-slideout.tsx: all display priceCents / 100
- "price contract" comment in all 3 display files
- `bun run typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-bug-fixes-polish/03-03-SUMMARY.md`
</output>
