---
plan: 3
phase: 2
title: "Verify Already-Fixed Bugs + Color Tokens + Cascade Typecheck Guard"
requirements: [BUG-01, BUG-02, BUG-03, BUG-07]
must_haves:
  - "BUG-02: priceInput field name confirmed present in ToothSlideoutData (no priceCents anywhere)"
  - "BUG-03: useEffect([toothNumber]) state reset confirmed present in tooth-slideout.tsx"
  - "BUG-01: status type is 'diagnosed' | 'planned' in SaveTreatmentInput (not 'proposed'); hardcoded 'diagnosed' default documented as intentional"
  - "BUG-07: bg-[#FFE97D], text-[#4A4018], hover:bg-[#F5DC60] replaced with bg-lemon, text-lemon-foreground, hover:bg-lemon-hover in tooth-slideout.tsx and $patientId.tsx"
  - "bun run typecheck passes with zero errors after all Phase 2 changes"
wave: 2
depends_on: [1, 2]
files_modified:
  - apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx
  - apps/dentalemon/src/routes/_workspace/$patientId.tsx
autonomous: true
---

# Plan 3: Verify Already-Fixed Bugs + Color Tokens + Cascade Typecheck

## Tasks

### Task 1: Verify BUG-02, BUG-03, BUG-01; replace hardcoded lemon colors (BUG-07)

<read_first>
- apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx
- apps/dentalemon/src/routes/_workspace/$patientId.tsx
- apps/dentalemon/tailwind.config.ts
</read_first>

<action>
**Verification step — BUG-02 (priceInput rename already done):**
Confirm `ToothSlideoutData` in `tooth-slideout.tsx` has `priceInput?: string` and no `priceCents` field. No changes needed if correct.

**Verification step — BUG-03 (state reset already done):**
Confirm `useEffect` with `[toothNumber]` dependency at around line 58-66 of `tooth-slideout.tsx` resets all form state fields. No changes needed if correct.

**Verification step — BUG-01 (status enum):**
Confirm `SaveTreatmentInput.status` in `use-save-treatment.ts` is `'diagnosed' | 'planned'` (not `'proposed'`). The hardcoded `status: 'diagnosed'` in `handleSaveToothData` in `$patientId.tsx` is the intentional default for new treatments — add a comment documenting this intent:

In `$patientId.tsx` inside `handleSaveToothData`, find the `saveTreatmentMutation.mutate(...)` call (now inside the `onSuccess` callback from Plan 2). Add a comment above `status: 'diagnosed'`:

```typescript
// Default to 'diagnosed' for new treatments — clinician confirms treatment during session.
// 'planned' is used for future treatments scheduled via treatment plan UI (future PR).
status: 'diagnosed',
```

**BUG-07 — Color token replacement:**

In `apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` (Save button, around line 297):
- Replace `bg-[#FFE97D]` → `bg-lemon`
- Replace `text-[#4A4018]` → `text-lemon-foreground`
- Replace `hover:bg-[#F5DC60]` → `hover:bg-lemon-hover`

In `apps/dentalemon/src/routes/_workspace/$patientId.tsx` (Continue to Payment button, around line 302):
- Replace `bg-[#FFE97D]` → `bg-lemon`
- Replace `text-[#4A4018]` → `text-lemon-foreground`
- Replace `hover:bg-[#F5DC60]` → `hover:bg-lemon-hover`

The Tailwind config already defines these tokens under `theme.extend.colors.lemon`:
- `DEFAULT: "#FFE97D"` → `bg-lemon`
- `hover: "#F5DC60"` → `bg-lemon-hover` (via `lemon.hover` key)
- `foreground: "#4A4018"` → `text-lemon-foreground` (via `lemon.foreground` key)

No changes to `tailwind.config.ts` needed.
</action>

<acceptance_criteria>
- `grep -n "priceCents" apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — no output
- `grep -n "priceInput" apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — at least one match
- `grep -n "useEffect" apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — match includes toothNumber in dependency array
- `grep -rn "bg-\[#FFE97D\]" apps/dentalemon/src/routes/_workspace/\$patientId.tsx apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — no output
- `grep -rn "text-\[#4A4018\]" apps/dentalemon/src/routes/_workspace/\$patientId.tsx apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — no output
- `grep -rn "hover:bg-\[#F5DC60\]" apps/dentalemon/src/routes/_workspace/\$patientId.tsx apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — no output
- `grep -rn "bg-lemon" apps/dentalemon/src/routes/_workspace/\$patientId.tsx apps/dentalemon/src/features/workspace/components/tooth-slideout.tsx` — two matches (one per file)
</acceptance_criteria>

---

### Task 2: Cascade typecheck guard

<read_first>
(No file reads required — run commands and interpret output.)
</read_first>

<action>
Run the cascade typecheck to verify all Phase 1 and Phase 2 changes compile together without errors:

```bash
cd apps/dentalemon && bun run typecheck
```

Expected: zero TypeScript errors.

If errors appear, fix them before marking this plan complete. Most likely error sources after type tightening:

1. **`ToothData.surfaces` type mismatch** — if `$patientId.tsx` constructs a `ToothData` with `surfaces: data.surfaces` and `data.surfaces` is still `string[]`, the ToothSlideoutData type must also be updated. Check `ToothSlideoutData.surfaces` in `tooth-slideout.tsx` — change `surfaces?: string[]` → `surfaces?: ToothSurface[]` (import from `five-surface-selector.helpers`).

2. **`buildToothMap` callers** — if any caller passes `state: string` instead of `state: ToothState`, narrow the type at the call site. Check `dental-chart.tsx` and any useDentalChart hook that builds the teeth array.

3. **`getToothColorClass` callers** — if any caller passes a raw string, add a type assertion or fix the upstream type.

4. **`Map<number, ToothState>.get()` returns `ToothState | undefined`** — if callers previously used the `Map<number, string>` return without undefined checks, they will now need a null-coalesce: e.g., `map.get(n) ?? 'healthy'`.

Fix all typecheck errors found. Re-run `bun run typecheck` until it passes with zero errors.

Document which files were additionally modified (if any beyond the three planned) in the SUMMARY.
</action>

<acceptance_criteria>
- `cd apps/dentalemon && bun run typecheck` exits with code 0
- No TypeScript errors in output
</acceptance_criteria>

---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tailwind token → rendered HTML | CSS class names are build-time resolved; no runtime security implication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | typecheck output | accept | TypeScript errors are build-time developer tooling only; no runtime exposure |
| T-02-07 | Tampering | color token substitution | accept | Pure visual string replacement; no logic change; no security boundary crossed |
</threat_model>
