---
phase: quick-260507-8zl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dentalemon/src/routes/_workspace.tsx
  - apps/dentalemon/src/routes/_workspace/$patientId.tsx
  - apps/dentalemon/src/features/workspace/components/treatment-table.tsx
  - apps/dentalemon/src/features/workspace/components/workspace-top-bar.tsx
  - apps/dentalemon/src/features/workspace/components/year-segment-control.tsx
  - apps/dentalemon/src/features/workspace/components/timeline-carousel.tsx
  - apps/dentalemon/src/features/workspace/components/resizable-divider.tsx
  - apps/dentalemon/src/features/workspace/hooks/use-treatments.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Workspace layout matches wireframe: top bar, year filter, 3D carousel, divider, treatment table, footer"
    - "No standalone DentalChart or tab system visible in main view"
    - "Patient name and safety floor (allergies/meds/conditions) display in top bar"
    - "Treatment table has wireframe columns: Tooth, Surface, Condition, Treatment Plan, Done, Status, Total"
    - "Carried-over treatments appear in dimmed section below separator"
    - "Year filter segments filter carousel visits"
    - "Carousel shows focal card with 16x4 tooth grid, flanking cards with perspective, and + card"
    - "Resizable divider allows drag to resize carousel/table split"
    - "Notes and TreatmentPlan accessible as sheet overlays from top bar icons"
  artifacts:
    - path: "apps/dentalemon/src/features/workspace/components/treatment-table.tsx"
      provides: "Extracted treatment table with wireframe columns"
    - path: "apps/dentalemon/src/features/workspace/components/workspace-top-bar.tsx"
      provides: "Patient avatar, safety floor, action icons"
    - path: "apps/dentalemon/src/features/workspace/components/year-segment-control.tsx"
      provides: "Apple-style year filter pills"
    - path: "apps/dentalemon/src/features/workspace/components/resizable-divider.tsx"
      provides: "Drag-to-resize divider between carousel and table"
  key_links:
    - from: "workspace-top-bar.tsx"
      to: "use-patient-profile.ts"
      via: "usePatientProfile hook"
      pattern: "usePatientProfile"
    - from: "workspace-top-bar.tsx"
      to: "use-medical-history.ts"
      via: "useMedicalHistory hook for safety floor"
      pattern: "useMedicalHistory"
    - from: "timeline-carousel.tsx"
      to: "tooth-slideout.tsx"
      via: "onSelectTooth callback"
      pattern: "onSelectTooth"
    - from: "$patientId.tsx"
      to: "treatment-table.tsx"
      via: "TreatmentTable component"
      pattern: "TreatmentTable"
---

<objective>
Align the workspace page ($patientId.tsx) and supporting components with the wireframe spec.

Purpose: The workspace works functionally but the layout diverges from the wireframe. This plan closes all visual/structural gaps — removes the tab system and standalone DentalChart, adds the top bar with safety floor, upgrades the carousel to 3D with embedded tooth grids, extracts a proper treatment table, and adds year filtering and a resizable divider.

Output: Workspace page matching the wireframe layout: TopBar -> YearFilter -> 3D Carousel -> Divider -> TreatmentTable -> Footer
</objective>

<execution_context>
@.planning/quick/260507-8zl-workspace-wireframe-alignment/260507-8zl-PLAN.md
</execution_context>

<context>
@apps/dentalemon/src/routes/_workspace.tsx
@apps/dentalemon/src/routes/_workspace/$patientId.tsx
@apps/dentalemon/src/features/workspace/components/timeline-carousel.tsx
@apps/dentalemon/src/features/workspace/hooks/use-treatments.ts
@apps/dentalemon/src/features/workspace/hooks/use-medical-history.ts
@apps/dentalemon/src/features/workspace/hooks/use-treatment-plan.ts
@apps/dentalemon/src/features/patients/hooks/use-patient-profile.ts
@apps/dentalemon/src/features/workspace/components/workspace-tabs.tsx
@apps/dentalemon/src/features/workspace/components/dental-chart.tsx

Key data hooks already available:
- usePatientProfile(patientId) -> displayName, firstName, lastName
- useMedicalHistory(patientId) -> entries with entryType (allergy/medication/condition) + active flag
- useTreatmentPlan({ patientId, branchId }) -> TreatmentPlanItem with carriedOver, conditionCode, visitId, surfaces
- useDentalChart -> tooth data for carousel cards and ToothSlideout
- Brand constants: CURRENCY_SYMBOL, APP_LOCALE, BRAND_GOLD
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip layout header to bare auth guard</name>
  <files>apps/dentalemon/src/routes/_workspace.tsx</files>
  <action>
Remove the header element with "Workspace" text from _workspace.tsx. Keep the h-screen flex-col shell, auth guard, and Outlet. The top bar with patient context moves into the child route ($patientId.tsx) in Task 5.
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && grep -c 'header' apps/dentalemon/src/routes/_workspace.tsx | xargs test 0 -eq</automated>
  </verify>
  <done>_workspace.tsx contains only auth guard shell with Outlet, no header element</done>
</task>

<task type="auto">
  <name>Task 2: Extract TreatmentTable component with wireframe columns</name>
  <files>
apps/dentalemon/src/features/workspace/components/treatment-table.tsx
apps/dentalemon/src/features/workspace/hooks/use-treatments.ts
apps/dentalemon/src/routes/_workspace/$patientId.tsx
  </files>
  <action>
Add `conditionCode` to the Treatment interface in use-treatments.ts (the API already returns it since save-treatment sends it).

Create treatment-table.tsx as a new extracted component with wireframe columns:
- Tooth | Surface | Condition | Treatment Plan | Done | Status | Total
- Surface column: treatment.surfaces?.join(', ')
- Condition column: treatment.conditionCode display name
- Treatment Plan column: treatment.description ?? treatment.procedureName
- Done column: checkmark icon when status === 'completed'
- Remove CDT column, rename Amount to Total
- Row hover: bg-[#FFE97D]/10 (gold-soft), active row: bg-[#F2F2F7]

Props: treatments array, onSelectTreatment callback, selectedTreatmentId, plus carried-over items (for Task 3).

Update $patientId.tsx to import and render TreatmentTable instead of inline table markup.
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && npx tsc --noEmit --project apps/dentalemon/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>TreatmentTable renders with all 7 wireframe columns. conditionCode on Treatment type. No inline table markup in $patientId.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Add carried-over treatment grouping</name>
  <files>
apps/dentalemon/src/features/workspace/components/treatment-table.tsx
apps/dentalemon/src/routes/_workspace/$patientId.tsx
  </files>
  <action>
In $patientId.tsx, import useTreatmentPlan({ patientId, branchId }). Filter items where carriedOver === true and pass as a separate prop to TreatmentTable.

In treatment-table.tsx:
- Render a "Carried Over" separator row in the table body after current-visit treatments
- Carried-over rows: dimmed background (opacity-60), "from {date}" label
- Cross-reference visitId against visits[] array to derive the date for display
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && grep -c 'carriedOver\|Carried Over' apps/dentalemon/src/features/workspace/components/treatment-table.tsx</automated>
  </verify>
  <done>Treatment table shows current treatments above separator and carried-over treatments below with dimmed styling and source dates</done>
</task>

<task type="auto">
  <name>Task 4: Remove WorkspaceTabs and standalone DentalChart from main view</name>
  <files>apps/dentalemon/src/routes/_workspace/$patientId.tsx</files>
  <action>
Remove from $patientId.tsx:
- WorkspaceTabs import and the tab bar div
- activeTab state and ALL tab-conditional rendering
- Standalone DentalChart rendering from main content area

KEEP:
- useDentalChart hook (tooth data feeds carousel cards in Task 8 and ToothSlideout)
- ToothSlideout (triggered from carousel card tooth clicks instead of chart clicks)
- All sheet components (MedicalHistoryForm, TreatmentPlanTab, etc.) — they become accessible from top bar icons in Task 5

Main content area below carousel becomes: only TreatmentTable from Task 2.

Notes (MedicalHistoryForm) and TreatmentPlan become sheet overlays triggered by top bar icons.
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && grep -c 'WorkspaceTabs\|activeTab' apps/dentalemon/src/routes/_workspace/$patientId.tsx | xargs test 0 -eq</automated>
  </verify>
  <done>No tab system or standalone DentalChart in main view. useDentalChart and ToothSlideout retained. Sheet components retained for top bar access.</done>
</task>

<task type="auto">
  <name>Task 5: Create WorkspaceTopBar component</name>
  <files>
apps/dentalemon/src/features/workspace/components/workspace-top-bar.tsx
apps/dentalemon/src/routes/_workspace/$patientId.tsx
  </files>
  <action>
Create workspace-top-bar.tsx with 56px height, frosted glass styling (backdrop-blur-xl bg-white/70):

LEFT section:
- Patient avatar chip with initials from usePatientProfile (firstName[0] + lastName[0])
- Display name text, chevron icon (display-only, no dropdown yet)

CENTER section:
- Safety floor banner: red-tinted bar (bg-red-50 border border-red-200)
- Shows active allergies, medications, conditions from useMedicalHistory (filter entryType + active flag)
- Each item as a pill/badge

RIGHT section:
- Visit date label
- Icon buttons row:
  - Rx (Pill icon) -> onRx callback
  - Notes/Edit (Pencil icon) -> onNotes (opens MedicalHistoryForm sheet)
  - Attachments (Paperclip icon) -> onAttachments
  - Treatment Plan (list icon) -> onTreatmentPlan (opens TreatmentPlanTab sheet)
  - Fullscreen (Maximize icon) -> toggle (cosmetic, low priority)

Props: patientId, visitDate, onRx, onConsent, onLab, onPmd, onAttachments, onNotes, onTreatmentPlan

In $patientId.tsx: render WorkspaceTopBar at the top of the page, wire all sheet-open callbacks to existing sheet state toggles.
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && npx tsc --noEmit --project apps/dentalemon/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Top bar renders with patient avatar, safety floor, and all icon buttons. Sheet overlays open when icons clicked.</done>
</task>

<task type="auto">
  <name>Task 6: Simplify footer to match wireframe</name>
  <files>apps/dentalemon/src/routes/_workspace/$patientId.tsx</files>
  <action>
Simplify the footer section in $patientId.tsx:
- Left side: "{pendingCount} pending" text + bold "{CURRENCY_SYMBOL}{totalAmount}" — use actual pendingCount (treatments with status !== 'completed'), not treatments.length
- Right side: Gold "Continue to Payment" button (keep existing)
- REMOVE the 5-icon action bar from footer (those actions moved to top bar in Task 5)
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && grep -c 'pendingCount\|pending' apps/dentalemon/src/routes/_workspace/$patientId.tsx</automated>
  </verify>
  <done>Footer shows only pending count, total amount, and payment button. No action bar icons in footer.</done>
</task>

<task type="auto">
  <name>Task 7: Add YearSegmentControl</name>
  <files>
apps/dentalemon/src/features/workspace/components/year-segment-control.tsx
apps/dentalemon/src/routes/_workspace/$patientId.tsx
  </files>
  <action>
Create year-segment-control.tsx:
- Apple-style segmented pills: derive unique years from visits[].createdAt, prepend "All"
- Props: years (string[]), selectedYear (string), onSelect (year: string) => void
- Style: rounded-full pills, active pill = white bg + shadow, inactive = transparent
- Compact height, centered layout

In $patientId.tsx:
- Add yearFilter state (default: current year as string)
- Derive unique years from visits array
- Render YearSegmentControl between top bar and carousel
- Filter visits by selected year before passing to TimelineCarousel (pass all if "All" selected)
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && npx tsc --noEmit --project apps/dentalemon/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Year segment control renders, filters visits by year, carousel updates accordingly</done>
</task>

<task type="auto">
  <name>Task 8: Upgrade TimelineCarousel to 3D perspective with tooth grids</name>
  <files>apps/dentalemon/src/features/workspace/components/timeline-carousel.tsx</files>
  <action>
Visual overhaul of timeline-carousel.tsx per wireframe:

FOCAL CARD (~520x270px):
- Elevated shadow, full opacity, gold accent border (BRAND_GOLD)
- 16x4 tooth grid (teeth 1-16 top row, 17-32 bottom, 4 rows total representing buccal/lingual/mesial/distal or similar grouping)
- Colored condition pips on teeth with data from useDentalChart
- Clicking a tooth cell calls onSelectTooth(toothNumber) -> opens ToothSlideout
- Card footer: visit date

LEFT FLANKING CARD (~300x210px):
- CSS transform: perspective(800px) rotateY(6deg) scale(0.82), opacity: 0.4
- 16x4 grid with empty/placeholder pips (no real tooth data)

RIGHT FLANKING / "+" CARD:
- NO tooth grid — centered "+" icon + "New Visit" label
- opacity: 0.2, dashed border
- Distinct from data cards

Layout: centered flex (not horizontal scroll), smooth CSS transitions between states.

New prop: onSelectTooth: (toothNumber: number) => void — replaces old chart's onSelectTooth.
Only the focal card uses real tooth data; flanking cards show placeholder grids.
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && grep -c 'perspective\|rotateY\|onSelectTooth' apps/dentalemon/src/features/workspace/components/timeline-carousel.tsx</automated>
  </verify>
  <done>Carousel shows 3D perspective layout with focal card (16x4 grid + real tooth data), flanking card with perspective transform, and + card. onSelectTooth wired.</done>
</task>

<task type="auto">
  <name>Task 9: Add ResizableDivider between carousel and table</name>
  <files>
apps/dentalemon/src/features/workspace/components/resizable-divider.tsx
apps/dentalemon/src/routes/_workspace/$patientId.tsx
  </files>
  <action>
Create resizable-divider.tsx:
- 8px tall bar with centered 36x5px pill handle
- cursor: row-resize styling
- onPointerDown/onPointerMove/onPointerUp handlers for drag tracking
- Props: onResize callback providing delta pixels

In $patientId.tsx:
- Add splitRatio state (default ~0.45 for carousel zone)
- Render ResizableDivider between carousel section and treatment table section
- Apply dynamic flex-basis via inline styles to both zones
- Enforce min heights for both zones (carousel min ~200px, table min ~150px) to prevent collapse
  </action>
  <verify>
    <automated>cd /Users/eladventures/Desktop/dentalemon && npx tsc --noEmit --project apps/dentalemon/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Resizable divider renders between carousel and table. Dragging changes split ratio. Both zones respect minimum heights.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/dentalemon && npx tsc --noEmit`
2. Layout match: open workspace side-by-side with wireframe HTML — top bar, carousel, divider, table, footer match
3. Tooth interaction: click tooth pip in focal carousel card -> ToothSlideout opens -> save -> treatment in table
4. Safety floor: allergies/meds/conditions render from useMedicalHistory in top bar
5. Year filter: switch years -> carousel filters correctly
6. Carried over: treatments from prior visits in dimmed section below separator
7. Notes/TreatmentPlan: accessible via top bar icons as sheet overlays
8. All existing sheets (Rx, Consent, Lab, PMD, Attachments, Payment) still open/close
9. Resizable divider: drag to resize, respects min heights
</verification>

<success_criteria>
- Workspace page layout matches wireframe: TopBar -> YearFilter -> 3D Carousel -> Divider -> TreatmentTable -> Footer
- No standalone DentalChart or tab system in main view
- All 4 new components created and wired
- All existing functionality preserved (sheets, tooth slideout, treatments)
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260507-8zl-workspace-wireframe-alignment/260507-8zl-SUMMARY.md`
</output>
