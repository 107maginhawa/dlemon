# Backend Handler Module Template

Reference layout for every module under `services/api-ts/src/handlers/`.
Deviation from this layout requires a comment explaining why.

## Sub-domain Bucketing Threshold

| Handler count | Rule |
|---|---|
| < 10 handlers | Stay flat at module root (`dental-billing`, `dental-imaging`) |
| 10–19 handlers | Flat OR bucketed — judgment call |
| ≥ 20 handlers | **MUST** bucket by sub-domain |

`dental-patient` (46 handlers), `dental-visit` (~26), `dental-clinical` (~27) all require bucketing.

## Canonical Layout — Small module (< 20 handlers, stays flat)

```
handlers/<module-name>/
├── <verb><Entity>.ts              # Handler files — flat at module root
├── <verb><Entity>.test.ts         # Co-located test for that handler
├── <module>.test.ts               # Module-level integration tests (stay at root)
├── <domain>.fsm.property.test.ts  # FSM property tests (stay at root)
├── repos/
│   ├── <entity>.schema.ts
│   ├── <entity>.repo.ts
│   ├── <entity>.test.ts
│   └── <entity>.facade.ts
└── utils/
    ├── <entity>-validators.ts
    ├── <domain>.service.ts
    └── <helper>.ts
```

## Canonical Layout — Large module (≥ 20 handlers, bucketed)

```
handlers/<module-name>/
├── <module>.test.ts               # Module-level integration tests — stay at root
├── <domain>.fsm.property.test.ts  # FSM property tests — stay at root
├── repos/                         # Unchanged — schemas + repos + facades
├── utils/                         # Validators + helpers
└── <sub-domain>/                  # One folder per sub-domain
    ├── <verb><Entity>.ts          # Handler
    └── <verb><Entity>.test.ts     # Co-located test moves WITH its handler
```

### Sub-domain naming

Use noun plurals: `identity/`, `contacts/`, `insurance/`, `treatment-plans/`, `recalls/`, etc. Match the domain language, not the HTTP verb.

### What stays at module root (never moves into a sub-domain folder)

- Module-level integration tests (`<module>.test.ts`, `<module>-<concern>.test.ts`) — they span multiple sub-domains by design
- FSM property tests (`<domain>.fsm.property.test.ts`)
- `repos/` and `utils/` subdirectories

### Test co-location rule

Co-located handler tests (`<handler>.test.ts`) **move with their handler** into the sub-domain folder. Module-level integration tests stay at root.

## How sub-domain bucketing works with the generator

`registry.ts` is generated by `scripts/generate.ts`. The generator uses `findHandlerFile()` (added in Phase 11) which:
1. Recursively walks `handlers/{module}/**/*.ts` to find a handler by operationId
2. Uses the found path (subfolder or flat) for the import statement
3. Falls back to flat path for modules not yet bucketed

**After bucketing a handler:** run `bun run generate` and commit the updated `registry.ts` alongside the file moves. This keeps CI consistent.

## Rules

### No `index.ts` barrel exports

Handlers are imported by name in `registry.ts`. Barrel files add indirection without benefit.

### `repos/` is for schemas and repos only

- `<entity>.schema.ts` — Drizzle schema definitions
- `<entity>.repo.ts` — repository class with typed query methods
- `<entity>.test.ts` — unit tests for the repo (use real DB, not mocks)
- `<entity>.facade.ts` — thin read-only facade exposing data to other modules

Nothing else belongs in `repos/`. Validators and helpers go to `utils/`.

### `utils/` is for non-handler, non-repo supporting code

- Input validators (Zod schemas used by handlers)
- Domain service logic shared by multiple handlers in this module
- Pure utility functions specific to this module

Tests for `utils/` files are co-located in `utils/` next to the file under test.

## Naming conventions

| Purpose | Pattern | Example |
|---------|---------|---------|
| Handler | `<verb><PascalEntity>.ts` | `createDentalPatient.ts` |
| Handler test | `<verb><PascalEntity>.test.ts` | `createDentalPatient.test.ts` |
| Module integration test | `<kebab-module>.test.ts` | `dental-patient.test.ts` |
| Schema | `<kebab-entity>.schema.ts` | `patient-contact.schema.ts` |
| Repo | `<kebab-entity>.repo.ts` | `patient-contact.repo.ts` |
| Facade | `<kebab-scope>.facade.ts` | `visit-billing.facade.ts` |
| Validator | `<kebab-entity>-validators.ts` | `contact-validators.ts` |
| Service | `<kebab-domain>.service.ts` | `visit.service.ts` |

## Reference Modules

| Module | Handler count | Layout | Notes |
|--------|---|---|---|
| `dental-scheduling/` | ~13 | Flat | Has `repos/` + `utils/` |
| `dental-billing/` | ~18 | Flat | Has `repos/` + `utils/` |
| `dental-imaging/` | ~8 | Flat | Has `repos/` |
| `dental-patient/` | 46 | **Bucketed** (Phase 11) | 8 sub-domains |
| `dental-visit/` | ~26 | **Bucketed** (Phase 11) | 6 sub-domains |
| `dental-clinical/` | ~27 | **Bucketed** (Phase 11) | 9 sub-domains |
