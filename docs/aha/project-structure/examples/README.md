# Example Artifacts (Foreign Repository)

> ⚠️ **These files were NOT generated against this repository.**

The outputs, migration plans, and reports in this folder were imported alongside
the AHA project-structure prompt pack (commit `62c06216`) and describe a
**different codebase** (references to `apps/memberry`, `apps/admin`,
`railway.json`, root `QUICKSTART.md`/`ROADMAP.md`/`VERTICAL_TDD.md`, `.audits/`
logs — none of which exist here).

They are kept **only as format/quality references** for what each prompt's
output should look like.

**Do not treat anything in this folder as an approved migration plan for this
repo.** Prompt 03 (`../prompts/03-approved-docs-and-root-cleanup-execution.md`)
must only execute plans generated fresh against this repository, located at:

- `../outputs/` — prompt 01 + 02 inventories
- `../migration-plans/` — prompt 01 + 02 migration plans
- `../reports/` — prompt 03 + 04 execution/validation reports

## Contents

- `outputs/` — sample DOCS_INVENTORY, PRD_INDEX_DRAFT, PROJECT_STRUCTURE_INVENTORY (+ `old/` historical samples)
- `migration-plans/` — sample DOCS / PROJECT_STRUCTURE migration plans (+ `old/`)
- `reports/` — sample cleanup + link-validation reports
