# AHA Execution Tracker (post-decision)

**Created:** 2026-06-12 · **Branch:** `chore/workflow-verification-sweep` · **Source:** [`product-decisions.md`](./product-decisions.md) §6 + [`consolidated-remediation-roadmap.md`](./consolidated-remediation-roadmap.md) §7/§8 · **Protocol:** Vertical TDD (tests-first, vertical slices) per `docs/development/VERTICAL_TDD.md`.

Check items off as completed. Tracks 1 & 2 are **decision-free and parallelizable now**. Track 3 is unblocked by the 2026-06-12 product-decision session. Track 4 routes to future specialized prompts (not 04).

---

## Track 0 — Pre-flight confirmations (before their dependent items)
- [x] **0069 populated-DB exposure = NONE** (verified 2026-06-12, high confidence). Investigated runner mechanics + write-path history + release records: the importPMD write-path was structurally callable 27 days before 0069 (rows *could* be created without `source_description`), BUT every environment is always-fresh (VERSION `0.2.0.0` pre-release, "release" workflow cuts GitHub releases only, all DBs created via `db:reset`/`seed-demo` post-migration) — no persistent DB ever carried a row across the 0069 boundary. Audit's own roadmap had this `[NEEDS CONFIRMATION]`; confirmed. **Decisive mechanic:** the migrator (`drizzle-orm/node-postgres` `migrate()` in `core/database.ts`) is single-transaction + journal-watermark-gated → a forward-only migration CANNOT rescue a DB blocked at 0069 (chain halts there); editing 0069 in place is the only real fix and is safe (never re-runs on a DB past 0069). Authored the corrective migration regardless (see Track 1 last item).
- [x] Confirm exact **session-TTL** value for ADR-007 (~15–30 min idle) before the data-gov/org session work. — CONFIRMED 2026-06-13: the shared-device idle timeout is the **PIN-session inactivity timeout** = `INACTIVITY_TIMEOUT_MS = 5 min` (`apps/dentalemon/src/lib/pin-session.ts`), already implemented and *more* conservative than the 15–30 min estimate → satisfies C-3. (Better-Auth `sessionExpiresIn`=7d is a separate absolute layer.) Decoupled from org Batch C (owner-reset PIN + provisional gate don't touch session timing); the C-3 data-gov ADR-007 sign-off can cite **5 min**.

## Track 1 — Decision-free P1 slice (START HERE, parallel)
- [x] **dental-scheduling** — `listAppointments` + `appointment-patient.facade` add `isNull(deletedAt)` filter (soft-archived calendar leak). RED: archive appointment → absent from GET list/detail. (schema fix #3) — DONE 2026-06-12: both facade reads filter `isNull(deletedAt)`; 2 RED→GREEN tests; 219 scheduling + 75 retention pass; both typechecks clean. Backend-only (no HTTP archive trigger → no Hurl; FE trusts backend → no FE/E2E; wire shape + SDK unchanged).
- [x] **dental-pmd Batch B** — extend snapshot to safety-floor + demographics (narrowed set per decision #6); attestor = visit's **treating dentist** (decision #5). RED: snapshot includes floor+demographics; checksum stable. — DONE 2026-06-12: snapshot now carries `safetyFloor` (active allergies/meds/conditions, ordered for determinism) + narrowed `demographics` (name/DOB/sex); `authorMemberId` bound to `visit.dentistMemberId`; new `getSafetyFloorForPMD` facade; 4 RED→GREEN tests; 504 pmd+visit pass; both typechecks clean; MODULE_SPEC §7.1 reconciled to narrowed V1 truth. Wire shape (content:string) + SDK unchanged.
- [x] **dental-pmd Batch C** — partial `unique(visit_id) WHERE generated` + upsert/FOR UPDATE (race); add-only safety-floor merge; **finish mig-0063** (add Drizzle model `imported_pmd_safety_floor_events` + switch merge to append-only INSERT events, decision #20). RED: concurrent double-completion → exactly one generated PMD. (schema fix #4) — **MODULE COMPLETE** (C1 + C2-a + C2-b + Batch D all done).
  - [x] **C1** DONE 2026-06-12: partial unique index `pmd_document_visit_generated_unique` on `(visit_id,status) WHERE status='generated'` (mig 0102) + `generatePmdForVisit` catches the 23505 race and resolves idempotently (returns the winner). RED concurrent-double-completion 2→1; 2 new tests; full suite 3766 pass / 0 regress (1 pre-existing unmergePatients). Reverted transactional supersede — incompatible with openTestTx (nested db.transaction COMMITs the outer test tx); index + handler catch suffice.
  - [x] **C2** — split 2026-06-12. (a) **orphan-table reconcile DONE** with the 0069 migration-safety slice (commit `72954e12`): added the Drizzle model `imported_pmd_safety_floor_events` (exact 0063 shape, explicit FK name) + idempotent reconcile migration `0103_fat_miek.sql` (`CREATE TABLE IF NOT EXISTS` / FK `DO`-guard / `CREATE UNIQUE INDEX IF NOT EXISTS` — no-op on every DB that ran 0063). Snapshot now tracks the table; db:generate is consistent; 0103 idempotency pinned in `imported-pmd-0069-migration-safety.test.ts`. (b) **append-only MERGE MECHANISM + FIX-003 consumer DONE 2026-06-13** (commit `0f871ef3`): new `mergeImportedPMDSafetyFloor` op (POST `/dental/pmd/imported/{id}/merge-safety-floor`) surfaces an imported PMD's safety items into the patient's med-history as append-only entries → they reach the clinical Safety Floor (closes the "imported safety data is clinically inert" P1; `markSafetyFloorMerged` finally a caller). Idempotent via the flag (fast 409) + `imported_pmd_safety_floor_events` unique index (23505→409). **§15:** the FE preview already read top-level `{conditions,medications,allergies}` string arrays → backend extractor mirrors it EXACTLY (previewed==merged); BR-022 honored (imported row immutable, data copied forward as new rows; reconciled DOMAIN_MODEL "never merged"). FE confirm now imports AND merges + invalidates the safety-floor cache (honest "updated" claim). Mechanism+consumer ONE slice (no mechanism-only commit). 3-lens: 2 mutations caught (insert no-op + flag-flip); no blockers/majors. Gate: backend merge 6/0, pmd hurl 31/31, FE pmd 11/0, root tsc 0, lint 0. **Roadmap:** top-level-shape-only extraction (`.safetyFloor` round-trip deferred); content uncapped; no transactional atomicity (openTestTx → med-history-first ordering).
  - [x] **Batch D** — honest PMD E2E. DONE 2026-06-13 (commit `e4b86e59`). **J20** (`20-pmd-import-merge.journey.spec.ts`) replaces the API-only pmd-import false-green with a real rendered-UI journey: top-bar PMD button → viewer sheet → import form → preview → confirm, then proves via an INDEPENDENT safety-floor read that the imported allergy surfaced (mutation-proven non-vacuous: skip merge → J20 RED). Gate: J20 PASS live (`--project=journeys`, 8.6s). **Scoping note:** the complete-visit→auto-generate→viewer UI journey was NOT added — the generation trigger is already backend-pinned (Batch A `pmd-generation-trigger.test.ts` 2/0) and the top-bar+viewer path is E2E-covered by J20; a UI generation journey needs a destructive seed mutation (completing a load-bearing active visit, gated by completion rules) for marginal coverage → deliberately deferred.
- [x] **data-governance Batch C** — retention read API (FR8.14) + erasure/legal-hold TypeSpec role fix `["user"]→["admin"]` (own TypeSpec→regen). Contract pins: GET retention status 200 admin / 403 non-admin. (schema fix #5, cross-cutting fix #4) — DONE 2026-06-12.
  - [x] **retention read API (FR8.14)** DONE 2026-06-12: new `dental-retention.tsp` (`getRetentionStatus` GET `/dental/retention-status`, admin, tag `Retention`→`handlers/retention/`) wrapping existing `summarizeRetentionEnforcement`; handler + 4 RED→GREEN tests (401/403/200 never-run/200 enforced); SDK regen; **contract 6/6** (admin 200 / non-admin 403 verified end-to-end through generated `authMiddleware({roles:["admin"]})`); api-ts+root typecheck 0. Commit `a1fc138c`.
  - [x] **erasure/legal-hold role fix** DONE 2026-06-12: flipped 8 ops `#["user"]→#["admin"]` (5 erasure + 3 legal-hold; module docstrings already said "Admin-only"). All handlers already enforce admin → contract-correctness + middleware defense-in-depth, no behavior change. Regen: 8 routes now `roles:["admin"]`. Backend 49/0; **contracts erasure 33/33 + legal-hold 21/21** (admin gate now at middleware, zero regression); typecheck 0.
- [x] **0069 corrective migration + migration-safety test** — DONE 2026-06-12 (commit `72954e12`). Per the Track-0 mechanic, edited 0069 **in place** to the safe 3-step (add nullable → backfill `'Imported before provenance tracking'` → `SET NOT NULL`); end-state + snapshot unchanged. Added `imported-pmd-0069-migration-safety.test.ts` (applies the real 0069 SQL to a populated pre-0069 fixture in a throwaway schema: RED on the old single-statement form → GREEN on the rewrite; + NOT NULL invariant pin). Added the missing `ADD COLUMN ... NOT NULL` (no DEFAULT) rule to `scripts/lint-migrations.ts` (the exact gap that let 0069 ship; only-match before fix, satisfied after; already gated in `quality.yml`). **Gate:** migration-safety 3/3 + dental-pmd 122/0; full chain `0000→0103` rebuilds clean; real server boot applied 0103 on dev `monobase` 0102→0103; `lint:migrations` 104/0; api-ts + root typecheck clean; eslint clean. **Closes Track 1.**

## Track 2 — Decision-free module Batch B/C passes (parallel; roadmap §8 orders 8–18)
- [x] **dental-visit Batch B** — carry-over FE affordance + returning-patient E2E. DONE 2026-06-12 (commit `57b96bef`). Batch A (FIX-001 contract reconcile) was already landed 2026-06-11; §15 re-verified the SDK shape correct. **⚠ Mechanism deviation (real product-rule conflict the fix-ready plan missed):** the visit-completion gate (`updateDentalVisit:136`, VISIT_HAS_OPEN_TREATMENTS) forbids completing a visit with diagnosed/planned treatments → a *completed* prior visit retains none → carry-over **auto-discovery carries nothing** (verified it defeats even the demo seed: `completeVisit` resolves planned→performed, so Ana/Elena's "carry-over source" visits complete at performed and their auto carry-over is inert). The functional, FR1.11 path is **restore-dismissed** (dismiss-to-defer survives the gate → restore next visit via `restoreDismissedIds`, the Maria seed pattern). Shipped: `CarryOverPrompt` at the new-visit entry point (restore the previous visit's deferred/dismissed treatments) + `useCarryOverTreatments` + `usePreviousVisitDeferred` (dismissed-id source; getTreatmentPlan excludes dismissed) + a **treatment-table coherence fix** (carried rows were double-counted in the Grand Total — main list now excludes `carriedOver` rows). **Gate:** table coherence RED→GREEN, prompt 5/5 + deferred-hook 2/2, full FE 2336/0, returning-patient restore-dismissed **E2E passes live**, both typechecks + eslint clean. Backend untouched. **FOLLOW-UP for roadmap:** the carry-over *auto-discovery* path + the demo seed's Ana/Elena "planned-on-completed" carry-over scenarios are dead under the completion gate — flag for a product/seed reconcile (Ana's demo carry-over visibly carries 0).
- [x] **dental-clinical B/C/D/E/F** — consent revoke+history (P1); Rx list+dispense/cancel (P1); amendments-visibility FR1.16 (P2); consent-template picker FIX-009 (consume dental-org, do NOT rebuild backend); lab-status enum + attachment-size doc reconcile. **MODULE COMPLETE** (A–F all done).
  - [x] **Batch B (FIX-004 + FIX-005)** — DONE 2026-06-12 (commit `8c869db5`). Consent revoke + history "History" tab in `consent-sheet.tsx` (Signed/Pending/Revoked forms + informed refusals; Revoke gated to pending + `canRevoke`=dentist_owner/associate). **§15 caught a real contract drift** (ConsentForm response carries `revoked`/`revokedAt`/`revokedBy` but TypeSpec omitted them) → fixed the contract (added the 3 read-only fields + regen) instead of an FE cast; hurl `$.revoked` now schema-backed. Contract +6 scenarios (revoke PATCH→200 / sign-after-revoke 422 CONSENT_FORM_REVOKED / re-revoke 409 / refusal 201 / refusals-list 200 + step-8 CONSENT_FORM_SIGNED pin). New `consent-revoke-ui.spec.ts` E2E. Gate: FE 2340/0, consent-sheet 10/0, contract 47/0, E2E 5/0, consent backend 53/0, typechecks (FE+api-ts+sdk-ts) + lint + boundaries clean. Incidental regen: stale erasure/legal-hold JSDoc role comments `user`→`admin` caught up to Track-1 `a1fc138c` (comment-only). Facades/V-CLN-010 untouched.
  - [x] **Batch C (FIX-006)** — Rx list + dispense/cancel lifecycle in `rx-sheet.tsx` (P1, WF-016). DONE 2026-06-12 (commit `c25eed44`). **§15 caught the Batch-B drift class BOTH ways** (Prescription response + UpdatePrescriptionRequest omitted `status` though the wire carries/accepts it) → fixed the CONTRACT (new `PrescriptionStatus` enum + status on response model + optional status on update request + regen), not an FE cast; no invalid-enum-at-route was pinned at 422 so additive request enum is safe. FE: mode toggle (New | Prescriptions) + per-visit list with status badges + Dispense/Cancel **FSM-gated (pending only)** AND **role-gated (`canManage`=dentist_owner/associate)** + optimistic flip. Handler simplified to read validated `body.status` (dropped redundant raw parse + dead `INVALID_PRESCRIPTION_STATUS` guard). **Per-patient list NOT built** (no per-visit route → only per-visit; cross-visit would need a new backend route — deferral). 3-lens adversarial review = no blockers/majors (actioned MINOR 1/2 + NIT 3/5). Gate: FE 2346/0, rx-sheet 14/0, contract 52/0 (+5 FSM pins), E2E 1/1, dental-clinical backend 209/0, typechecks (FE+api-ts+sdk-ts) + lint clean.
  - [x] **Batch D (FIX-007 + FIX-008)** — amendments-visibility FR1.16 + Notes/MH role parity. DONE 2026-06-12 (commit `10f28217`). **§15 found NO contract drift** (read contract/SDK already match the handler — first clean §15 of the series), but surfaced a real **FE create-path bug with TWO masked defects**: the only UI create path sent `originalRecordId=''` (empty UUID→400) AND omitted `visitId` from the body (validator requires it). Per the user's AskUserQuestion decision (**Option 1 — fix create + read list, full E2E loop**): NEW read-only `amendments-list.tsx` (visit-scoped, unwrap `data.data`, deterministic newest-first + stale-guard) mounted in the tooth-slideout read-only area; wired a valid `originalRecordId` (selected tooth's treatment id) + gated "Add Amendment" on it; added `visitId` to the createAmendment body + dropped the masking cast. **FIX-008** = matrix-verified visibility PIN (Notes/Attachments are view+supervised-draft entry points → current render correct; added `staff_full` parity pins, no button removed). Genuine UI write→read E2E loop (read a pre-filed amendment back + file one via UI → reads back). 3-lens adversarial review = no blockers/majors (closed the E2E loop, made list order deterministic, tightened wording; flagged 3 pre-existing gaps for roadmap). Gate: FE 2357/0, contract 52/0, E2E 3/0 (loop 2/2), backend amendment.repo 7/0, typechecks (FE+api-ts) 0, lint 0, no regen.
  - [x] **Batch E (FIX-009)** — consent-template picker: surface per-clinic `body` wording (FR8.4b). DONE 2026-06-12 (commit `c8bfcb89`). **§15 corrected the fix-ready premise** ("hardcodes CONSENT_TEMPLATES" was STALE — dental-org FIX-004 already wired the picker to read clinic templates `{id,name}`; the const is ALSO name-only, so nothing was "going blank"). **Real gap:** the picker dropped the template's single plain-text `body` (the actual per-clinic consent wording; required string on the SDK type + handler + Drizzle). FE-only fix (no drift, no regen): `ConsentTemplateOption += body?`; `$patientId` map passes `body`; selecting a configured template renders a **read-only "Clinic consent wording" panel** (mode-gated, resets on close, XSS-safe plain-text). **AskUserQuestion forks:** Q1=**read-only reference panel** (not snapshot-onto-form — deferred roadmap; templates are versioned so `templateId` ref is traceable); Q2=**graceful fallback + "configure in Settings" nudge** (industry norm — never block care). `ConsentForm` has the 5 ADA fields but NO body field → persisting body needs a contract change (out of FE-only scope). 3-lens adversarial review = 2×SHIP + 1×SHIP_WITH_NITS, no blockers/majors (mutation-tested the new tests = honest; actioned the empty-string guard NIT). Backend untouched (§11 honored — dental-org owns the CRUD). Gate: FE 2362/0, consent-sheet 15/0, contract `dental-org` 33/0, NEW cross-module E2E 1/0 (owner POSTs a real template → picker surfaces its body), typechecks (FE+api-ts) 0, lint 0 errors, no regen.
  - [x] **Batch F (FIX-010)** — lab-status enum + attachment-cap doc reconcile. DONE 2026-06-12 (commit `fc42d0bd`). **CLOSES the dental-clinical module.** §15 = code-truth verification across **three** enforcement layers BEFORE editing: lab enum drift is **one-sided** (code + MODULE_SPEC correct; only API_CONTRACTS wrong: `sent/received/completed/rejected`→`ordered/in_fabrication/delivered/fitted/cancelled`, initial `pending`→`ordered`, DE-015 on `→delivered` not "completed"). Attachment cap = **layered reality matching NO doc** (5MB/50MB/25MB docs vs FE 50MB guard vs storage 100MB-nonDICOM/2GB-DICOM real cap vs createAttachment metadata-only no-cap). **AskUserQuestion (a) docs-only vs (b) add-cap → user picked "good-for-user + feasible + standard" = (a):** (b) rejected because a 50MB clinical cap would orphan the already-uploaded S3 object (storage accepts 100MB), and storage is the standard single byte-enforcement point (shared with imaging CBCT 2GB). Reconciled 3 docs (API_CONTRACTS lab+attachment, MODULE_SPEC WF-039.4, form-contracts) to code truth; rewrote the attachment section (it's a JSON metadata-register step-4-of-4, not multipart/presigned); corrected the FE comment falsely claiming "50MB matches backend"; added a backend pin locking metadata-only no-cap. Gate (lighter — docs-only, no behavior change): backend 25/0 (new pin) · FE attachments-sheet 12/0 · typecheck (FE+api-ts) 0 · lint 0 · no regen · no contract/E2E. Roadmap flag: rest of API_CONTRACTS still snake_case/stale (bounded by a new field-casing note).
  - **ROADMAP FLAGS from Batch D** (pre-existing, not in scope): (a) `_workspace` route guard does not enforce `canAccess(role,'workspace')` — non-workspace roles can reach the workspace shell; (b) `SoapNotesSheet`/`AttachmentsSheet` do not client-gate Save/upload by capability (backend 403 is the hard gate) — view-only roles get an editable form; (c) `AmendmentRepository.findMany` + `TreatmentRepository.findByVisit` lack `orderBy` → non-deterministic across fetches (user-facing amendment list now FE-ordered; backend `orderBy` is the durable fix; also de-fragiles the hurl `data[0]` assertion).
- [x] **dental-billing B/C/D — MODULE COMPLETE** (A–E + B/C/D all landed). *(payment session-derived `recordedByMemberId` per #8 is a SEPARATE handler-trust fix — NOT done here; claims Phase-2 / receipt-email / auto-discount / tax-engine all out of scope.)*
  - [x] **Batch B (FIX-003 + FIX-004)** — discount-apply + per-payment-void affordances in `invoice-detail.tsx`. DONE 2026-06-12 (commit `36727bf3`). **§15 (5-front parallel sweep) = ONE drift:** FIX-003 applyDentalDiscount FE_ONLY (request/full-invoice response already declared; unit trap = `percentageRate` is a 0–100 PERCENTAGE not cents/fraction; coherent re-render via invalidate+refetch the enriched GET); FIX-004 voidDentalPayment CONTRACT_FIX (wire returns `voidedByMemberId` via `.returning()`, TypeSpec omitted it — clinical-B precedent → added `voidedByMemberId?: UUID` + regen, not a cast). OWNER-ONLY gate via NEW `canApplyDiscount`/`canVoidPayment` (`=== 'dentist_owner'`, stricter than canWrite) sourced from org-context store; per-payment void is soft-delete (row persists flagged "Voided", restored balance from refetch), distinct from whole-invoice void. **3-lens adversarial (SHIP/SHIP_WITH_NITS, 7 mutations confirm non-vacuous); majors folded pre-commit:** fixed a `userEvent` number-input flake (→ atomic `fireEvent.change`), made the voidReason roadmap flag REAL (TypeSpec comment + report §B7), tightened hurl discount pins to EXACT arithmetic. **ROADMAP FLAG:** voidReason lacks `@minLength` + handler has no trim guard → empty-reason reversal reachable via direct API (deferred = server-behavior change; documented). Gate: backend 5/0, contract 40/0 (+exact discount arithmetic + voidedByMemberId), FE 2372/0 (+10 new, stable ×3), E2E 5/0 (+discount money journey), typechecks (FE+api-ts+sdk-ts) 0, lint 0, boundaries clean.
  - [x] **Batch C (FIX-005 + FIX-006)** — payment-plan create dialog + flag-sync. DONE 2026-06-12 (commit `8f956193`). **§15 confirmed both:** (a) added `installments[]` to `DentalPaymentPlan` + regen → dropped the FE `as unknown` cast + view DERIVES paid/remaining/count/nextDue from installments; (b) **FIX-006 = REAL bug FIXED** — the new patient-facade setter is the SOLE writer of `hasActivePaymentPlan` (was permanently false → patients with live plans wrongly archivable); single `syncPatientActivePaymentPlanFlag` recompute (hasActive=on_track|behind) hooked into create/setStatus/updatePlanStatus, GUARDED multi-plan clear, boundary-legal via `.facade`; verified NO terminal path bypasses these 3 methods; honest RED→GREEN pin (real plan, not hand-forced). NEW `PaymentPlanCreate` dialog (2-24, NO amount, owner/associate, legible errors) + footer entry. Fixed pre-existing `formatPlanStatus` 'onTrack'→'on_track' + reconciled installment-status maps to the pending|paid|overdue|waived enum. **Create-only for V1** (manual status op orphaned/unmounted; status system-driven). 3-lens SHIP×2+SHIP_WITH_NITS, vacuousTestRisk NONE, mutation-tested; folded the InstallmentStatus enum coherence finding. **ROADMAP FLAGS:** installment-payment lifecycle disconnect (no path marks installments paid → derived Paid stays ₱0 for live plans; completed unreachable in prod); createDentalPaymentPlan trusts body.patientId (handler-trust, like #8); unconditional re-sync = intentional self-healing. Gate: backend flag-sync 3/0 + no regression (fsm/jobs/repo/property/idempotency/edge/acceptance/dental-billing/EC1 all green), contract 40/0, FE 2383/0 (+19 new), E2E 6/0 (+create journey), typechecks (FE+api-ts+sdk-ts) 0, lint 0, boundaries clean.
  - [x] **Batch D (FIX-009/010/011/012)** — DONE 2026-06-12 (commit `3cad3041`). FIX-009 zero-membership report pin ×5 (GREEN on arrival, non-vacuous: empty asserted vs a 2-org-seeded DB); FIX-010 balance-equality pin (`outstandingBalanceCents == Σ non-voided balanceCents`, voided excluded both sides) + flagged `PatientBalanceResponse` contract drift (0-consumer orphan); FIX-011 aged demo seed (45/78/120d → days30/60/90+ buckets); FIX-012 collections-summary documented backend-only. No product code (tests/seed/docs) → no 3-lens. Gate: cross-tenant 10/0, balance 1/0, typecheck 0, lint 0. **Overdue-filter E2E DEFERRED → Track 4** (needs a test-only cron-trigger hook; fresh E2E org can't see demo-seed aged data; per Batch A §9).
- [x] **dental-patient Batch C** — comms-consent error surface, safety-floor equality pin, unmerge 500→501, plan-total. DONE 2026-06-12 (commit `6407f5a3`). **§15 = NO drift, no regen** (FIX-003 FE-only, FIX-004/005 test-only, FIX-006 value-only). **FIX-003** (GAP-4): silent comms-consent `.catch(()=>{})` → tested helper `patients/lib/communication-consent.ts` persisting via SDK + surfacing failures (error toast + Retry); registration POST left untouched (no route-wide migration). **FIX-004** (GAP-8): decision-neutral equality pin — `getDentalPatientSafetyFloor` == active-filtered `listMedicalHistory` (non-vacuous: inactive allergy excluded by both); GAP-6/#15 source-of-truth stays open. **FIX-005** (GAP-10): unmerge 500→501 + `code` pin — **closes the long-standing pre-existing unmergePatients failure** (handler returns clean 501/NOT_IMPLEMENTED; test was stale). **FIX-006** (TP-BR-006) [DECISION: **derive**, user-delegated]: §15 trace showed the fix-ready "plan→billing integrity" premise was FALSE — billing/workspace/case-pres already DERIVE money from item priceCents; header `totalEstimateCents` is a 1-consumer (plans-sheet) drift-prone denormalized field. Fix = single source of truth: new `recomputeTotal` (= Σ `getTreatmentPriceCentsByPlan`, same item set as `grandTotalCents`) at **every** link choke-point (present/approve/**accept**/treatment-trigger); itemless drafts keep the manual estimate. **3-lens caught a MAJOR** (`acceptCasePresentation` 3rd link caller missed recompute → live drift) → FIXED + late-link real-flow pin (load-bearing). Gate: backend patient 22/dental-patient 80/treatment-plan 26/case-pres 16/real-flow 5 + contract 127 (patient46/coord15/visit66) + FE 138 + helper 5; typechecks 0; lint 0; boundaries clean. **ROADMAP FLAGS**: item-delete/unlink total-staleness (same as recomputeStatus denormalized limitation); status-vs-total choke-points not identical sets (Lens-3 MINOR); GAP-6 alert source still a product decision. Batch B (statement) still gated on billing print (landed → runnable next).
- [x] **dental-audit — MODULE COMPLETE (Batch A finish + B + C)**. DONE 2026-06-12 (A `7b260884` / B `576590d7` / C `c1b55686`). **§15 killed a false premise** (same class as dental-patient FIX-006): journey 10 is named `10-void-amend-audit` but **amends a clinical note, not an invoice** — the fix-ready "extend the void+invoice fixture / assert `invoice.voided`" premise was FALSE. **FIX-002** (Batch A finish, E2E): extended journey 10 — after the signed-note addendum write, SPA-navigate (PIN session is in-memory; `page.goto` would log out) to Settings→Audit Log (owner-only; `pinAuth('dentist')` === dentist_owner) and assert the `visit_note.amended` row surfaces with actor (who) + addendum reason (why='Correction'). **Compliance call (user-delegated "what's industry-compliant"): asserting the signed-record amendment is the more HIPAA-true WF-028 proof** (§164.312(b) — altering the legal medical record is the textbook audit scenario; "who voided this invoice" is one instance of the same control). Mutation-tested non-vacuous (bogus action filter → RED). **FIX-003** (Batch B, comment/doc-only — verified zero executable change in the 98-caller file): purged the phantom "pg-boss consumer" + stale "Never throws" from `audit-logger.ts`; reconciled all 8 present-tense pg-boss/async claims in MODULE_SPEC to the ADR-005 inline-sync reality. **FIX-004** (Batch C, test+doc): new `legacy-sink-divergence.test.ts` (test-with-db harness) asserts `count(dental_audit)==count(dental_audit_log)==N` + per-action parity over mixed event types (§15: dual-write is unconditional across ALL types) — mutation-proven (disable legacy write → RED); + MODULE_SPEC §10c "Sink boundary" documenting the 3 sinks (dental_audit_log authoritative / dental_audit legacy / base `audit_log_entry`) + the intentional base-PHI-read invisibility in the dental viewer (Q2 input). 3-lens per test-touching batch. Gate: canary 1/0, journey 10 GREEN, audit-log component 7/0, typecheck (FE+api-ts) 0, lint clean, no regen (backend frozen). **Track 3 leftovers** (#17/#18, EXECUTION-TODO line 53): auditor-role owner-only resolve, base-PHI routing INTO the dental viewer + 3→1 sink merge, append-only trigger build, CSV export.
- [x] **notifications — MODULE COMPLETE (A-backfill + B + C)**. DONE 2026-06-12 (A-backfill `8e151da0` / B `adf1e994` / C `5f4b5f80`). **A-backfill (FIX-001 E2E):** producer→inbox E2E — a REAL `cancelBooking` notification (single-user host==client self-cancel, `recipient: user.id`, no auth switch) surfaces in the dashboard-shell bell → inbox → mark-read; non-vacuous (badge only renders when unreadCount>0). **Batch B (FIX-002 push opt-in):** `PushOptInPrompt` mounted in the **patient-workspace shell** (product-decision Q2 = prompt at first relevant clinical action, not login) — honest (renders nothing when push unconfigured; permission only on gesture; stops nagging after grant/deny/dismiss via localStorage); `usePushNotificationRouting` + pure `makeNotificationClickHandler`/`resolveNotificationDeepLink` map the push payload `{type,relatedEntity}`→deep link (booking/appt→/calendar, recall→/patients, billing→/billing, else /dashboard); DI adapters (no SDK mock → no pollution); OneSignal live delivery stays env-gated. 3-lens folded NITs (assert real `'billing'` producer type; z-40 below workspace sheets). **Batch C (FIX-003/004/005/006):** extracted `notification-classification.ts` (pure, repo delegates, behavior-identical). **FIX-003 §15 = LIVE seam** (reminder/recall email maps to tags absent from `EmailTemplateTags`, no `.hbs` → email-consented reminder queues a row that fails-at-process); in-scope honest fix = registry-invariant test (every mapped tag REGISTERED-or-documented-Phase-2 + Phase-2 set pinned exactly) + MODULE_SPEC Phase-2 flag, NOT building 4 templates (§11). **FIX-004 = product-decision OVERRODE fix-ready** (medical-priority fail-open, don't remove): replaced hardcoded `return false` with a real empty-set classifier `isMedicalNotificationType` (no quiet-hours/batching exists → fail-open already satisfied; no V1 medical types); non-vacuous (true for in-set type, false for every real producer type). **FIX-006 honesty pins** (clinic defaults can't override consent gate — resolver has no preferences input; 2 backend + 1 FE). **FIX-005** = authored `docs/product/modules/notifications/MODULE_SPEC.md`. 3-lens folded MINOR (18 not 19 types) + NITs. **Gate:** notifs backend 26/0 + reminder/recall consumers 51/0 (no regress), resolver 12/0, FE notifications+settings 50/0, notification-inbox E2E 1/1, api-ts + FE typecheck 0, lint 0 errors, **no contract/SDK/TypeSpec change** (no regen). **ROADMAP:** author reminder/recall email `.hbs` templates (then move tags out of Phase-2 list); fail-closed mapper option (return null for unregistered) deferred; medical types → `MEDICAL_NOTIFICATION_TYPES` when introduced; comms.* push → /dashboard fallback until a comms route exists.
- [x] **case-presentation — MODULE COMPLETE (Batch A + B + C)**. DONE 2026-06-12 (A `4a30d2b8` / C `7ba6751f`; Batch B FIX-002 viewer was landed earlier this session). **Batch A (FIX-001):** new `tests/e2e/journeys/19-case-presentation-accept.journey.spec.ts` (J19, registered in the harness roster) — DOM-drives present→e-sign→**accept** (decision=accepted, plan→approved, signer persisted) + the **reject** leg (decision=rejected, typed reason persisted, plan→rejected), each proven via an INDEPENDENT read of the durable aggregate. **§15 corrected the fix-ready premise** ("seed already has 4 plans — reuse"): `scripts/seed-demo.ts` seeds ZERO plan headers; the 4 cp plans come from `seed-supplement.ts` `cpPlanSpecs`, and they bind to patients by **non-deterministic `allPatients` physical-row index** (NOT demo P0..P9 order — here presented→Maria/draft→Miguel/rejected→Ana/approved→Carlos), so J19 **discovers** its targets by plan STATUS via the reader (robust across envs). Mutation-tested non-vacuous (drop the signature stroke → RED); GREEN clean (9.3s) + alongside J08/J09. **Batch C (FIX-004):** `docs/product/modules/case-presentation/MODULE_SPEC.md` (FSM + accept-needs-items precondition + approve/accept parity + option-acceptance ownership Q3 + GAP-6 telemetry do-not-fix + Q1/FIX-003 no-separate-estimate). Gate: J19 E2E GREEN+mutation-RED, typecheck FE+api-ts 0, lint 0 errors (no product/contract/SDK change → backend/contract/FE-unit untouched). **ROADMAP FLAG (pre-existing, NOT this batch):** the same physical-row binding lands `cp-rejected`'s linked implant on **Ana Reyes**, breaking J08 (hardcoded-patient decline) — J08 fails in isolation/first/zero-of-my-code → pre-existing, not a regression; durable fix = bind `cpPlanSpecs` by displayName.
- [x] **dental-imaging — MODULE COMPLETE (Batch A + B)**. Batch B DONE 2026-06-12 (feat `eda43dde` / docs-close this commit). FIX-003/004/005/006. **§15 caught a REAL production bug** (BUG-IMG class): `deleteImage` returned `200 {success:true}` but the generated SDK response transformer treats the success branch as `ErrorResponse` → `data.error.timestamp` **crashes** on the real body (any FE consumer threw). Root cause = deleteImage was the lone delete not returning 204 (siblings all 204 → transformer no-ops on empty body); TypeSpec already declares `void|ErrorResponse` → fix = handler→**204**, NO regen (updated 3 BE tests + hurl #30; archived side-effect still pinned). FE: `updateModality`+`deleteImage` mutations in use-image-library + reclassify-`<select>` (excludes cbct) + delete-with-confirm in `ImageMetadataEditor` (no FE role-gate — matches sibling updateMetadata; backend BR-026/027; no normalizeThrown → shouldRetry skips 4xx). **3-lens BLOCKER fixed**: `key={image.id}` on editor mount stops seeded modality/confirm/error leaking across images (mutation-tested key-remount pin). **FIX-004**: added `imaging_finding.create` audit row-written pin (file only had `.confirmed`); **§15 = createCephReport does NOT write an audit row** (Pino info only) so nothing to assert there; mutation-tested. **FIX-005**: second ceph patient = **Angela Reyes P19** (promised in patientDefs, never wired; seedCephChain filename-parameterised, additive); CBCT study already seeded (P4) → that half already satisfied; **runtime seed-coherence env-deferred** (MinIO down → study-create 500 ECONNREFUSED). **FIX-006**: MODULE_SPEC §7b documents imaging_finding (image-scoped FSM) vs dental_finding (visit-scoped vocab) are intentionally separate — do-not-merge + §10 library-admin routes/204. Gate: BE imaging 404/0 (isolated) + events 8/0, FE imaging 404/0, typechecks (FE+api-ts+sdk-ts) 0, lint 0, 3-lens SHIP; contract imaging hurl + image E2E **env-deferred** (MinIO/Docker absent; 204 pinned at BE-integration+unit). **Batches C/D/E decision-resolved OUT of V1** (#2 no-AI auto-detect declared out; #19 superimposition preview-only + CBCT finalize out) — unscheduled.
- [x] **dental-perio Batch B/C — MODULE COMPLETE (Batch A + B + C + GAP-5)**. DONE 2026-06-12 (Batch B/GAP-5/C-3 docs `c94b8d2c` · Batch C iPad CI `516f9b71`). Batch A (diagnosis-of-record persistence) was landed 2026-06-11. **§15 found most fix-ready premises STALE.** **FIX-004** (WF-P05 PDF export): verified genuinely unbuilt (no render/print route/button) → annotated **V2 DEFERRED** in WORKFLOW_MAP + MODULE_SPEC (deferred wins, matches STANDARDS_COMPLIANCE). **FIX-005**: premise doubly stale — all 7 BR-P01–P07 already in `br-registry.json` (with test sources), and the traceability script reads `docs/prd/BUSINESS_RULES.md` via `BR-\d{3}` (can't match `BR-Pxx`) and never reads the registry → added a scope note documenting the per-module registry as authoritative; `audit:trace` green (47 BRs, 0 untested). **FIX-006**: moved multi-exam comparison from STANDARDS_COMPLIANCE "deferred" → "implemented" (shipped 2026-06-07). **GAP-5/#2 (no-AI/voice):** §15 = voice is **mounted** in perio-chart-overlay but gated behind the **`perio.voice_charting` flag (default OFF**, compliance-pending) + browser-speech → default product is manual-only; reconciled MODULE_SPEC (over-claimed "built and shipped") + STANDARDS_COMPLIANCE (said "out of scope") to flag-default-off truth, added the real flag to §18, removed 2 vestigial never-wired flags; **no code touched** (doc-align only). **C-3**: documented perio chart as authority for recession/gingival-margin. **FIX-007 (Batch C):** §15 = spec NOT unconditionally skipped — runs+passes locally (4/4, 22s), already in `test:e2e` matrix; real gap is the DB-less CI `e2e` job → added a dedicated **`ipad-device-coverage`** CI job (postgres + api-ts boot + webkit, ipad projects only) so device assertions actually execute in CI (`continue-on-error` until first green, then promote). Gate (lighter — doc + test-infra, no product/contract/SDK, no regen): iPad 4/0 live, audit:trace green, YAML valid, zero `.ts/.tsx` changed. **ROADMAP:** voice mounted-but-flag-off + cloud-dependent → future gate/remove call; iPad job non-blocking pending first CI green.
- [x] **data-governance Batch B** — enforced-mode retention proof (dryRun:false through real RETENTION_TARGETS facades). DONE 2026-06-12 (commit `8db01774`). **CLOSES Track 2 §8.** FIX-003 = the never-run production seam: `RetentionPolicyRepository.findEnabled()` (real policy row) → `evaluateRetention(db, …, { dryRun:false })` with the **default** `RETENTION_TARGETS` + **default** `logAuditEvent` (NO injection) against a real seeded DB. New `retention-enforced-run.test.ts` proves end-to-end: (1) eligible aged attachment really soft-archived (`deletedAt` set via the real `archiveAttachments` facade); (2) a legally-held attachment (PATIENT_2 Person under hold) left untouched (`deletedAt` null); (3) a real `retention.enforced` row written to `dental_audit_log` (actor=`RETENTION_SYSTEM_ACTOR`, target=policy, metadata `actionedCount:1/legalHeldCount:1/dryRun:false`); (4) a pre-existing audit row survives (trail never purged). **§15 = NO facade bug surfaced** — the `dryRun:false` safety core is genuinely sound (the fix-ready's "RED may expose a real bug" did not bite). **GREEN-on-arrival → non-vacuous via 2 load-bearing mutations** (defeat legal-hold filter → held attachment wrongly counted/archived = RED; no-op the real archive write → `deletedAt` never set despite engine `actionedCount:1` = RED). Test-only → self-review (Batch-F/D precedent), no 3-lens. Gate (lighter): retention suite 42/0 (per-file isolated, +1 new), api-ts typecheck 0, lint 0 errors, **no regen / no contract / no E2E** (no handler/TypeSpec/SDK change). `logAuditEvent` + `placeLegalHold` verified openTestTx-safe (plain inserts on the passed db, no nested transaction).

## Track 3 — Newly-unblocked decision-gated batches (this session)
- [x] **data-governance Batch E** (#1, C-4) — governance admin UI + E2E. DONE 2026-06-12 (C-4 backend `d837a56c` + FE panel `cffadfda` + E2E `da612551`). **§15 = decision text two-sided stale** (decision #1's "owner-initiates + tenant-scoped" vs its own "Unblocks: admin UI + E2E" + the already-shipped `["user"]→["admin"]` RBAC + the shipped platform-wide-by-design list); erasure is gated to the Better-Auth **platform `admin`** role and the clinic app has no platform-admin area. **Resolution (user-delegated): admin-operated queue, no erasure-RBAC reopening** (fix-ready warned against speculative RBAC churn); clinic-owner-initiated + per-caller-org list scoping deferred to Phase-2 (documented). **C-4 (backend):** `requestErasure` now rejects person-only subjects (no patient anchor) → 4xx — closes the bare-person caller-supplied-tenant leak; every request carries a server-resolved tenant (FIX-001). Reworked `erasure-tenancy.test` (bare-person 201→4xx RED-proven; list-scoping uses 2 real patient subjects in 2 real orgs) + `erasure-legalhold.test` (patient anchor) + `dental-erasure.hurl` (real patient subject via subject-user self-onboard+register + person-only-reject pin, **36/36**). **GAP-1 (FE):** new **Data Erasure** Settings panel — platform-admin queue (list + status filter/badges + legal-hold flag) with Approve (anonymize-engine) / Reject (with reason) on `requested` rows; presentational `DataErasurePanel` (props, 9/0 unit) + thin container (useSession admin gate + generated react-query hooks); self-gates (non-admin → notice, not 403 wall; server hard-gates independently). NEW dev-only `POST /dev/promote-admin` (mirrors `/dev/verify-email`) for the E2E. 3-lens: security SHIP; folded the reason-input-lock-during-mutation finding + pin. Gate: erasure backend 40/0, hurl 36/36, FE data-erasure 9/0 + settings 86/0, E2E 1/1 (chromium, live API), api-ts+FE tsc 0, lint 0, no regen. **ROADMAP/Phase-2:** clinic-owner initiate surface + per-caller-org list scope + a tenant filter in the panel; GAP-8 dental-erasure MODULE_SPEC (P3).
- [x] **dental-clinical attachment erasure** (#7) — erasure facade + target: null filename/note + S3 delete. DONE 2026-06-12 (commit `cc29f221`). **CLOSES database-schema-audit P1 "dental_attachment PHI survives erasure".** §15 verified the invocation path BEFORE coding: `anonymizeSubject` iterates ALL `Object.values(ERASURE_TARGETS)` and auto-aggregates the report-shape's `fileIdsPendingS3Delete` → **registering a key is sufficient, no engine edit**; `approveErasure` threads the aggregate to the existing `physicalDeleteErasedFiles`. New `attachment-erasure.facade.ts` (mirrors imaging precedent): nulls `fileName`→[ERASED] + `note`→null, marks row deleted (COALESCE-preserved erasure time), covers already-soft-deleted rows, idempotent. **3-lens caught a REAL security BLOCKER** (security-auditor lens): `dental_attachment.filePath` is an **unvalidated client string with no FK** — FE happy-path stores the object-store key, but seed/legacy rows store free-form `/uploads/…` paths (`seed-demo.ts`; imaging `listPatientImages.ts:71` "legacy attachments stored by filePath"). My first cut handed those to `storage.deleteFile`, whose idempotent `DeleteObject` "succeeds" against a bogus key → **false-erasure with a clean `deleted:N` audit**; the path itself also embeds PHI and was retained. **FIXED inline** (TDD RED→GREEN): new `filterExistingStoredFileIds` (drops non-uuid candidates — the col is `uuid`, a legacy path would also raise a cast error — then confirms existence) so ONLY confirmed object-store keys are surfaced for S3 delete; redact `filePath`→[ERASED] on every row (handle captured in-memory pre-redact). 2 load-bearing mutations per seam (skip-PHI-null → RED; skip-confirmation → legacy/mixed RED). Tests: legacy-not-surfaced, orphan-uuid-not-surfaced, mixed, filePath-redacted (facade 7/0 + e2e 2/0). Also elevated the shared `erasure-storage.ts` S3-fail log warn→error (security lens MINOR — a pending un-erased object is alertable). **ROADMAP** (scoped out, shared with imaging / pre-existing): no operator-reachable retry for an S3-delete left pending after an outage; `physicalDeleteErasedFiles` counts a non-throwing DeleteObject as deleted with no HeadObject confirm; **durable pending-record gains urgency** (once filePath is redacted a genuinely-pending confirmed-key object is no longer recoverable from the row — the error log is the surviving record); true legacy-filesystem objects can't be keyed → validate-filePath-at-write / migrate. Gate: backend 50/0 (erasure+storage+imaging-erasure+attachment-repo, no regress), erasure contract **33/33** (server restarted), typecheck (api-ts + root) 0, lint 0, boundaries clean; no TypeSpec/SDK change → no regen. (schema fix #2)
- [x] **dental-pmd Batch E** (#4) — strip non-repudiation/checksum-sealed language; leave signature absent; document signing Phase-2. DONE 2026-06-12 (strip `49e1197c` + docs-close this commit). **§15 = layered one-sided drift** (as predicted): `sign()` 0 production callers + signature/signed_at always NULL, BUT the signature FIELD is in the contract — and the TypeSpec `@example` was a **fake "Signed PMD" with status=signed + signature `MEYCIQDsX3...`**, the most actively-misleading client-facing artifact in openapi.json. **Fork resolved by decision #4 + risk → keep-and-relabel, NO AskUserQuestion:** `sign()` is exercised by 5 repo unit tests + has DB columns → removing = destructive migration + deleted tests; "leave signature absent" = keep the Phase-2-reserved stub, strip only the CLAIMS. Stripped across **5 layers** (TypeSpec docstring/example/enum/field-comments; backend generatePMD non-repudiation comment + repo header/`sign()` + schema header/columns/enum; FE viewer signed-banner; docs MODULE_SPEC ×4 + **NEW §8b "Signing posture"** resolving the dangling "see §-signing note" ref + API_CONTRACTS field-table/envelope + ui-prototype). Kept accurate "checksum-sealed integrity (tamper-evidence)" — only the false **non-repudiation/digital-signature** claim removed. Wire types byte-identical → SDK regen **description-only** (verified no type drift); openapi.json fake signature gone. **Honest-deferral pin** (non-vacuous regression guard): generated PMD asserts `status != 'signed'` + signature/signedAt null. Gate (no behavior change → self-review, not 3-lens): dental-pmd 45/0 (+pin), repo `sign()`-stub 18/0, trigger 2/0, FE viewer 5/0, root typecheck 0, lint 0 err, **pmd hurl 27/27** (fresh :7213). Remaining pmd work: ~~Batch C2-b append-only merge mechanism + FIX-003 consumer~~ DONE 2026-06-13 (`0f871ef3`); ~~Batch D honest E2E~~ DONE 2026-06-13 (`e4b86e59`, J20). **dental-pmd MODULE COMPLETE.**
- [x] **dental-org Batch C** (#9) + **provisional-org PHI gate** (C-1) — owner-reset-only PIN recovery; restrict `status=provisional` orgs from PHI writes until activated. DONE 2026-06-13 (3 commits `c51e0705` #9 owner-reset PIN / `68e64c41` C-1 gate+activate backend / `4faa4119` C-1 FE banner+seed/E2E wiring). **§15** corrected the #9 premise (owner-reset already the FE direction; the gap was a missing Staff-settings reset affordance for existing members → exposed `resetPin` + StaffEditModal section; self-service security-question handlers left orphaned per #9). **C-1** = `assertOrgLive` **production-only** gate (user-delegated "best judgment"; mirrors createOnboarding sibling guards, closes the prod-PHI hole, zero hurl/seed churn) at PHI roots createDentalPatient+createDentalVisit + owner-only idempotent audited `activateOrganization` endpoint + `OrgStatus`/status contract + `ClinicActivationBanner` + seed/E2E activation wiring. **Pre-flight:** session-TTL = 5 min PIN-idle (already implemented, decoupled — see Track 0). Gate: gate 4/0, activate 6/0, banner 4/0, dental-org hurl 35/35, reset-PIN + clinic-activation E2E PASS, db:reseed both demo orgs `live`, tsc 0, lint 0. Full detail in dental-org-fix-report Batch C addendum.
- [x] **dental-visit GAP-2 build** (#13, ⚠ scope expansion) — treatment-template create/apply UI; **keep** the seed. DONE 2026-06-13 (3 commits: `ca6527c2` Slice 0 contract → `179f1d8a` Slice 1 settings panel → `298394ec` Slice 2 apply affordance + E2E/docs-close this commit). **§15 found FIX-001 INCOMPLETE — the load-bearing pre-work was real:** FIX-001 (landed 2026-06-11) reconciled the template body shapes but **missed `listTreatmentTemplates`'s required `?branchId=`** — the op declared NO `@query` param so the generated SDK had `query?:never` and could not send the param the handler requires → bare `throw new Error` (500). Same createMember-class drift FIX-001 fixed for `getTreatmentPlan` but not here. **Slice 0 = the reconcile BEFORE wiring** (regen window safe — 0 FE consumers verified): added `@query branchId:UUID` (+ ApiBadRequestResponse) so the SDK sends it and the generated query validator returns a clean 400; also `applyTemplate` ApiOkResponse→**ApiCreatedResponse (201)** (handler returns 201 + hurl pinned 201 — doc honesty; SDK response type unchanged, transformer still runs at 201); dropped the now-stale `treatments:'stub'` seed cruft + lying comment (validator accepts `items[]` post-FIX-001). RED→GREEN hurl pin: GET without branchId → 400 (was 500). **Slice 1 = Settings → Treatment Templates panel** (mirrors sibling ConsentTemplates): owner-only create/edit/soft-delete with a dynamic items editor; **peso→cents unit trap pinned** (1200→120000). **Slice 2 = workspace apply affordance** — `ApplyTemplateButton` mounted in `workspace-table-zone` ABOVE the table (fix-ready Q2 "treatment-table area") gated on `currentVisitId && !isReadOnly` so it's reachable on an **empty** visit (the primary populate-from-template case the table's empty-early-return would otherwise block); owner/associate-only (parity w/ backend `assertBranchRole`), role-gate in an outer wrapper so non-clinical roles never fire the template fetch; on success invalidates visit treatments + plan aggregate (same keys as carry-over). **3-lens (3 parallel reviewers): both "MAJOR" findings VERIFIED-AWAY** — the "cancelled-visit write gap" hallucinated a non-existent status (enum is draft/active/completed/locked/discarded; applyTemplate's completed/locked guard exactly matches `createDentalTreatment.ts:51`), and the "empty-items wrong error shape" is invalid (errors.ts:320/367 maps ZodError→400 ValidationError). Folded 2 genuine NITs (split component to avoid wasted fetch for non-clinical roles; Edit-absent test pin). **Q2/Q4 inline** (Q2 default taken; Q4 lock-period is FIX-004/Batch D, untouched). Gate: dental-visit contract **67/67**, backend treatment-templates 32/0, settings panel 8/0 + workspace apply 4/0, full FE **2455/0**, create→apply **E2E 1/1** (chromium, live), typecheck FE+api-ts+sdk-ts 0, lint 0, regen diff template-scoped only. **ROADMAP (deferred nits):** backend template create/update/delete gate only on branch membership (no owner role) — FE gate is stricter, backend parity is a hardening gap; `deleteTreatmentTemplate` returns `{success:true}` vs TypeSpec `{}` (0 consumers); create/update return the raw row superset (createdBy/updatedBy) vs the TreatmentTemplate model (harmless); applyTemplate `byTooth` items omit `carriedOver` (pre-existing FIX-001 residual).
- [x] **dental-patient contact-edit build** (#14, ⚠ scope expansion) — expose `contactInfo` on getDentalPatient, editable + **audited**. DONE 2026-06-13 (`719df174` logger recursive-redaction prereq → `8b1882f9` contract+backend read/edit/audit → `834df644` FE edit form → `29444a94` E2E → docs-close). **§15 all 3 premises confirmed:** contactInfo col existed but getDentalPatient deliberately omitted it (→ contract+regen, not FE-only); edit rides Batch-A demographics path; updateDentalPatient had NO logAuditEvent (→ added `patient.contact.update`). **Track-4 logger prereq landed FIRST** (recursive walking redactor replacing Pino single-level `*.field` globs; 3-lens folded 2 majors — Date→`{}` and shared-ref→false-`[Circular]`; `no-console` handler lint guard). Backend: contactInfo on read **only when set** (V-PAT-014 exclusion pins still hold) + partial merge + PHI-free audit; 3-lens Lens-2 verified audit PHI-free + no list/export/create leak. FE read path was already wired (always-empty pre-API). Gate: logger 13/0, dental-patient 83/0, hurl 49/49, FE edit-form 9/0 + profile-page 15/0, E2E chromium 1/1, tsc/lint/boundaries clean. **Resolves Batch-A decision-queue (V-PAT-014 phone/email) via option (b).** ROADMAP: no explicit single-field clear (V2); empty-`{}` body no-op audit (FE-unreachable).
- [x] **dental-clinical GAP-5** (#11) — allergy blocking-with-override FE confirm-dialog (backend unchanged). DONE 2026-06-12 (feat `97a179ab`). **§15 = decision-resolved + the FE allergy source confirmed client-side** (`useMedicalHistory` — the same safety-floor cache the top bar renders) → option (a) **true pre-submit gate** is clean FE-only (no AskUserQuestion needed). `$patientId` sources active allergies → `patientAllergies` prop so `RxSheet` stays prop-pure (its global.fetch harness has no QueryClient). `matchAllergyConflicts` mirrors `createPrescription.ts:51-62` (case-insensitive substring both directions; trims defensively; skips empty allergens); `handleSave` gates BEFORE the POST; the Radix confirm dialog (`pre-completion-checklist` pattern — no shadcn AlertDialog in repo) "Prescribe anyway" is the only override (threaded as an arg, race-free); submit `onClick={handleSave}`→`() => handleSave()` so a MouseEvent can't spoof the override. Post-save server banner retained for **drug-drug interactions** (server-only) + as a divergence fallback. **3-lens (mutation-tested, not self-review) caught a real MAJOR** (Lens-1): override originally discarded the *entire* server `allergyConflicts` → a stale-cache server allergen the clinician never saw was swallowed; **fixed** to **set-based suppression** (suppress only the acknowledged allergens; un-acknowledged ones still surface post-save), mutation-pinned. Lens-2 clean (react-query dedupes the key w/ the top bar; prop-pure preserved); Lens-3 = all mutations killed (non-vacuous). Gate: rx-sheet **24/0**, FE **2420/0**, allergy block→override→verify-created **E2E 2/2**, typecheck (FE+api-ts) 0, lint 0; backend UNCHANGED → no TypeSpec/SDK/contract step. **ROADMAP:** the pre-submit block **fail-opens** when the medical-history cache is cold/errored (`patientAllergies=[]`) → degrades to the post-save advisory; bounded (visible safety-floor badges + server advisory still fire) and hard-gating Save on query state would block prescribing during an MH read outage — flagged not fixed.
- [x] **dental-audit auditor-role + sink** (#17/#18) — DONE 2026-06-13 (`f561388e` feat + `bd33f664` docs + this docs-close). **§15 = both premises corrected:** #17 viewer ALREADY owner-only (`assertBranchRole(['dentist_owner'])` + 403/cross-tenant pins) AND the append-only trigger (mig `0080`) + FIX-004 canary ALREADY landed → #17 collapsed to a ROLE_PERMISSION_MATRIX reconcile (owner-only binding, auditor role Phase-2). #18 = read-time single-pane union (NOT write-routing, NOT the 3→1 merge): `getAuditEvents` unions sink #3 base `data-access` PHI reads (the 8 dental read handlers write to `audit.logEvent`) scoped by **actor ∈ active branch members** (base table has no branch/tenant col), `details` never selected (PHI-safe), marked `metadata.source='base'`, FE "platform" tag. Caught + fixed a REAL 500 (freeform action filter hit base ENUM `audit_action`). 3-lens folded (no-leak positive control, details-absent pin, dropped `resolvedTenant` echo); rejected a false pagination BLOCKER. Gate: viewer 14/0 (mutation-RED proven), FE settings 87/0, audit hurl 20/20, tsc/lint/boundaries clean, no regen. **dental-audit MODULE fully closed.** Defer to V2: 3→1 merge, legacy `dental_audit` sunset, auditor read-only role, resource-scope hardening, CSV export.
- [x] **Doc-alignment / park passes** — DONE 2026-06-13 (7 commits `fadd2214`→`40702356`). **CLOSES Track 3.** Each premise re-verified against live code first (§15 applies to docs); 5 pure-doc commits (self-review) + #3 (FE pin) + #6 (contract @example + regen + honesty pin). Per-item:
  - [x] **#2** (imaging `fadd2214`): code verified aligned — `perio.voice_charting` flag default OFF (already reconciled `c94b8d2c`), imaging `FakeDetector` is a flag-gated (`dental_imaging_auto_landmark` default OFF → 403 FEATURE_DISABLED) dev/test fixture, **not** AI. STANDARDS_COMPLIANCE + perio MODULE_SPEC already aligned; remaining gap = imaging WF-030 read as if auto-detect ran automatically → rewrote to manual-default + a no-AI note + §18 flag description. (Perio needed NO change.)
  - [x] **#6** (pmd `40702356`): MODULE_SPEC §7.1 + API_CONTRACTS already narrowed (Batch B); §15 found 2 remaining drifts → fixed: **PRD FR12.1** (wide set + "digitally signed" → narrowed V1 + checksum-sealed + Phase-2-deferred list) and the **TypeSpec `@example`** (carried a FAKE top-level `diagnoses` field + top-level `allergies` — corrected to the true `demographics/safetyFloor/treatments/prescriptions` shape; regen example/description-only, sole tracked drift = a JSDoc on `PmdDocument.content`). Honesty pin (dental-pmd.test.ts) locks the narrowed shape (no top-level diagnoses/identifiers/allergies). Gate: dental-pmd 46/0, pmd contract 31/31, tsc FE+api-ts+sdk 0.
  - [x] **#3** (claims `0e3df8a3`): **§15 premise STALE** — there is NO FE claims create-path to disable (verified grep: zero `createClaimDraft`/`createInsuranceClaim` consumers; worklist = read + submit/remit lifecycle only). So: regression pin in `claims-worklist.test.tsx` (green-on-arrival, non-vacuous — a create button/testid flips RED) + Phase-2 PARKED label on billing MODULE_SPEC + patient-side `createClaimDraft` documented dormant (must not wire independently). Gate: claims-worklist 5/0.
  - [x] **#10** (org `9d909ea1`): §1.1 — multi-branch data model is day-one but V1 UI is single-branch (no branch create/switcher; verified grep); PRD §2.5 "day-one" is aspirational for the model, not a V1 UI commitment.
  - [x] **#12** (clinical `28bef908`): §10b park note — inventory/occlusion/post-op-template ops (~10) are V1-dormant (no PRD anchor, zero FE consumers verified) → keep handlers/tests, do NOT wire FE [DO NOT OVERBUILD]. Clarified consent-refusals + drug/allergy cross-checks are NOT dormant (V1-live).
  - [x] **#16** (patient `d206f527`): §1.1 — households read-only in V1 (`getPatientHousehold`/HouseholdCard), write ops (create/add/remove) parked Phase-2 (zero FE consumers verified). MODULE_SPEC previously omitted households entirely.
  - [x] **#19** (imaging `fadd2214`): WF-030b superimposition is preview-only in V1 (`id:null`; persist trio Phase-2-dormant, 0 consumers); CBCT finalize chain declared OUT of V1 (`finalizeCbctStudy` 0 prod consumers, seed/test only). Added to §13 edge cases.
  - [x] **C-2** (org `9d909ea1`): canonical active member_status = {invited,active,inactive}; 'revoked' is documentation-reserved Phase-2 (in Drizzle enum for forward-compat, no live transition — `transitionStatus` rejects it). Reconciles the 3-system disagreement without a destructive migration.
  - [x] **C-3** (governance `ca30134d`): AG-5 session-TTL RESOLVED = PIN idle **5 min** (`pin-session.ts INACTIVITY_TIMEOUT_MS`) + cloud 7 days (ADR-003 §Addendum). **§15 caught a phantom citation:** the session-expiry ADR is **ADR-003**, not ADR-007 (ADR-007 = self-service onboarding) — corrected the misnumber in DATA_GOVERNANCE. Also reconciled the stale "signed PMD is non-repudiable" governance language to Batch-E checksum-sealed posture (decision #4 coordination).

## Track 4 — Platform fixes (future specialized prompts, NOT 04)
- [ ] **buildTestApp** validator-mounting harness + raw-handler lint guard — RED a known drift (BUG-IMG-001 / consent-template) under the new helper first, then migrate incrementally (Hurl stays backstop).
- [x] **core/logger.ts** JSONB-recursive redaction + lint guard — DONE 2026-06-13 (`719df174`, landed as the load-bearing prereq for #14). Recursive walking redactor (`formatters.log`) replaces Pino single-level `*.field` globs; redacts PHI keys at any depth incl. arrays; builds a redacted copy (no caller mutation); path-based cycle guard. 3-lens folded 2 majors (Date→`{}`; shared-ref→false-`[Circular]`). Lint guard: `no-console` (all methods) in `src/handlers/**` (zero churn). Tests: logger 13/0.
- [ ] Flip **contract.yml** Hurl + Schemathesis to blocking — after clearing the 8 env-dep failures (MinIO/Mailpit/Stripe).

## Standing gotchas (carry into every batch)
- **§15 caveat:** verify SDK type vs handler shape (and contract vs handler) **BEFORE** wiring; treat any fix-ready "backend ready" as unverified.
- **Vertical TDD mandatory:** RED → GREEN → refactor; one module fully end-to-end before the next; gate = backend unit + contract + FE unit + E2E all pass.
- dev DB password = `password`. Backend per-file tests: `cd services/api-ts && DATABASE_URL=postgres://postgres:postgres@localhost:5432/monobase_test bun scripts/test-with-db.ts <file>`.
- Root `bun run typecheck` covers both workspaces; also run api-ts `bunx tsc` directly.
- **Never** run server/contract/E2E against `monobase_test` (pollutes the template); `db:generate` only from `services/api-ts` cwd.
- TypeSpec change → regen routes/validators → SDK regen is a **separate** step.
- Restart the :7213 server before `test:contract` (stale server masks drift).
