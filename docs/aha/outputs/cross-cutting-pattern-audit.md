# AHA Cross-Cutting Pattern Audit

**Generated:** 2026-06-12 · **Branch:** `chore/workflow-verification-sweep` · **Prompt:** `docs/aha/prompts/05-cross-cutting-pattern-audit.md` · **Scope:** read-only (no fixes). Synthesized from 13 module gap-plans + 13 fix-reports + 13 fix-ready plans + live-code evidence agents.

## 1. Inputs Reviewed

| Input | Details |
|-------|---------|
| Module audit index | 13 module/group cross-cutting extractions reviewed (case-presentation, data-governance, dental-audit, dental-billing, dental-clinical, dental-imaging, dental-org, dental-patient, dental-perio, dental-pmd, dental-scheduling, dental-visit, notifications). |
| Gap plans reviewed | All 13 modules' `docs/aha/module-gap-plans/*-gap-plan.md` (cited per-pattern in INPUT A). |
| Fix-ready plans reviewed | All 13 modules' `*-fix-ready-plan.md` (cited per-pattern). Treated as PLANS, not proof of completion. |
| Completed fix reports reviewed | 13 `docs/aha/module-fix-plans/*-fix-report.md`. Only Batch-A (and dental-billing's Batch-E) work is treated as Fixed; later batches are Not-Completed regardless of plan existence. |
| KG used | Yes — evidence agents used the existing knowledge graph (fan-in counts, facade exports, orphan/consumer rows). Not regenerated for this audit. |
| KG refreshed | No — `/understand` not re-run; KG spine dual-operationId false-orphan hazard is noted as a known tooling backlog item. |
| /understand-domain used | Yes — domain-graph existed and was consulted by evidence agents (workflow ownership, FSM consistency). |
| /understand-domain refreshed | No — domain-graph not regenerated this pass. |
| Webwright used | No — Webwright was NOT run for this audit (evidence dir empty). See §10. |
| Playwright/E2E inspected | Source-only — E2E spec files were READ (e.g. lab-order-tracking-api.spec.ts, imaging-cbct.spec.ts, journey specs) but NO browser/Playwright run was executed for this audit. |
| Shared/platform files inspected | Yes (INPUT B live-code investigations): `core/logger.ts`, `core/jobs.ts`, `core/audit-logger.ts`, `core/audit-phi-sanitizer.ts`, `generated/openapi/routes.ts` + `validators.ts` + `registry.ts`, `app.ts`, `eslint.config.js`, `scripts/test-with-db.ts`, `scripts/run-contract-tests.ts`, `apps/dentalemon/src/test-setup.ts`, `.github/workflows/contract.yml`, RBAC TypeSpec modules, FSM/cancel/erasure handlers. |
| Limitations | No runtime verification (no Webwright/Playwright/server boot); branch gated green at start of audit but green ≠ contract-complete (input-only validation, non-blocking CI contract checks). Each module executed at most one fix batch (dental-billing did two: A+E; case-presentation shipped its Batch B / FIX-002 while its Batch-A journey E2E was deferred); all later batches are plans, not proof. KG/domain-graph are a few commits stale. |

## 2. Fix Status Interpretation

| Module/Group | Gap Plan Exists? | Fix-Ready Plan Exists? | Fix Report Exists? | Interpreted Status | Notes |
|--------------|------------------|------------------------|--------------------|--------------------|-------|
| case-presentation | Yes | Yes | Yes | Partially Fixed | PASS grade. FIX-002 shipped (shared signed-acceptance/plan-version viewer, FE-only). FIX-001 journey E2E deferred; FIX-003 estimate blocked; FIX-004 MODULE_SPEC doc-only deferred. openapi-null-drop left open (routed to platform codegen queue). |
| data-governance (erasure + legalhold + retention) | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (erasure tenancy hardening) done. Batches B/C/D/E not done. Per-module tenancy fix did NOT remove platform-layer cause (handlers still accept tenancy from body for bare-person/unresolvable subjects). |
| dental-audit | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A viewer shipped via settings-panel registry. FIX-002 (E2E), FIX-003 (doc sweep), FIX-004 (sink-divergence canary) not executed — sink-parity, RBAC matrix drift, stale-async docs all remain open. |
| dental-billing | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (overdue/plan-Behind cron) + Batch E (receipt + shared PrintableDocument primitive + receipt TypeSpec drift reconcile) done. Batches B/C/D not done. recordedByMemberId + claims vertical decision-blocked. |
| dental-clinical | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (Lab/PMD top-bar buttons + lab-order E2E relabel) done. Batches B/C/D/E/F not done. Dead-trigger root cause flagged explicitly for this prompt; consent-revocation FE undelivered. |
| dental-imaging | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Healthiest large module. Batch A (kill-switch zero-rows pin + retry:false) done. Batch B planned; C/D/E blocked on product decisions. sdk-ts retry/normalizeThrown root cause left open. |
| dental-org | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Built+owns the NEW settings-panel registry; reconciled consent-template contract (escalated P2→P1 real bug). Batches C/D blocked on product decisions. raw-handler harness root cause unfixed. |
| dental-patient | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (demographics edit via new Person facade) done. Batch B (statement) + all of Batch C not done. Pino JSONB-PHI flagged repo-wide; dead contact-info UI surfaced. |
| dental-perio | Yes | Yes | Yes | Fixed (Batch A) / Partially Fixed overall | Batch A flipped PARTIAL PASS → PASS (diagnosis persistence via migration 0101 + completion-response drift fixes). Batch B (docs/BR registry) + Batch C (iPad spec) not done. |
| dental-pmd | Yes | Yes | Yes | Partially Fixed | Audit decision FAIL; fix-report COMPLETE for Batch A only (generation trigger on visit-completion hot path). FIX-002 content, FIX-003 safety-floor merge, FIX-004 E2E, FIX-005 signing all open/deferred/blocked. Reachability depends on dental-clinical top-bar batch. |
| dental-scheduling | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (cancel vertical: affordance + PATCH-cancel audit/event parity + policy alignment) done. Batches B/C/D not done; FIX-006 smoke tool deferred. Backend-complete-affordance cluster root cause open. |
| dental-visit | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (contract reconcile, spec-only, 66/66 Hurl) done. FIX-002/003/004/005/006 not implemented. Accepted-plan viewer (GAP-3) satisfied-pending-wiring by case-presentation FIX-002. |
| notifications | Yes | Yes | Yes | Partially Fixed | PARTIAL PASS. Batch A (in-app bell/inbox/badge) done — closed the write-only-subsystem in-app gap. Batches B/C not done. G4 deliveredAt + G2 settings relabel were Fixed in PRE-AHA batches (3/4), not this sweep. |

## 3. Recurring Patterns

| Pattern | Affected Modules/Groups | Severity | Scope Label | Evidence | Root Cause | Recommended Platform Fix |
|---------|-------------------------|----------|-------------|----------|------------|--------------------------|
| **raw-handler-test-blindspot** (tests mount raw handlers, skip generated zValidator → request-shape contract drift ships) | dental-org, dental-visit, dental-billing, dental-imaging, dental-clinical, dental-patient, dental-scheduling, dental-audit, data-governance, dental-pmd, notifications (11 of 13 cite it) | P1 | V1 RECOMMENDED (guard) | INPUT B: `createPerson.test.ts:34` / `imaging-integration.test.ts:123` mount `handler as any` with no validator; 203 of 307 handler test files are raw-handler-only; all 17 dental-imaging files have 0 validators; `dental-imaging.hurl` case 20b comment documents it. dental-org B3, dental-visit reconcile #1 (create-template 400 via validator before handler), dental-billing E3 (receipt HTTP-200-only hurl); dental-pmd "E2E specs drive APIs/mount components directly" (Still Open). | No shared `buildTestApp` helper (307 tests each roll their own); generated `*ResponseSchema` in validators.ts (62 defs) are unused by routes; no lint/CI guard forbids mounting a raw handler. This is the **mechanism that lets every apiok-contract-drift bug ship**. | (1) One exported `src/tests/build-handler-app.ts` mounting handlers WITH generated validators (reuse `registerRoutes`/per-op registrar). (2) Migrate drift-history modules first (dental-imaging 17 files, 0 validators). (3) ESLint rule flagging `app.post/get(path, handler)` in `*.test.ts` without a validator. (4) Keep Hurl as authoritative FE↔BE gate. |
| **self-consistent-fiction FE error mocks** (FE tests mock SDK errors with invented nested shapes instead of the real flat `SdkError` envelope → FE↔BE error-envelope drift, e.g. "New Visit → Please try again") — the FE-side twin of raw-handler-test-blindspot | dental-audit, dental-clinical, dental-billing, notifications, dental-patient (5 modules cite the discipline) | P2 | V1 RECOMMENDED (guard) | INPUT A: dental-audit fix-ready §5 "use the real SDK error/envelope shapes … no self-consistent-fiction mocks"; dental-clinical "real flat-envelope `SdkError`, not nested fictions"; notifications "`makeSdkError`-style real-envelope mocks"; the prior live `extractApiError` parser bug (flat `.body` envelope vs assumed nested `err.error`). | FE error-path tests assert against invented error shapes; no shared `makeSdkError` factory is enforced, so BE returns one envelope while FE tests trust another and the mismatch ships as a generic "try again" toast. | Promote a shared `makeSdkError` test factory + a convention/lint requiring SDK-error mocks use the real flat envelope; document in CONTRIBUTING_FRONTEND. Same incident-chain as raw-handler-test-blindspot — fix the BE detection hole and the FE mock discipline together. |
| **apiok-contract-drift** (handler returns envelope `{templates}` / flat-vs-nested while contract declares bare `ApiOkResponse<T>`) | dental-org, dental-billing, dental-visit, dental-perio, dental-patient, dental-scheduling | P2 | V1 RECOMMENDED (guard) | INPUT B: `models.tsp:334` proves bare-body convention; `routes.ts` has 513 input zValidators, 0 response validators; `registry.ts:376` untyped object literal. 3 real drifts: dental-org consent `{templates}/{template}/{deleted}`, billing receipt flat-vs-nested, dental-visit `listTreatmentTemplates` bare-array-vs-`{templates}`. `.github/workflows/contract.yml:123-129` Hurl + Schemathesis both `continue-on-error: true`. (dental-clinical's API_CONTRACTS lab-status enum doc-drift is a related doc-reconcile variant of this class, kept module-local in §6.) | Handler return values (`Promise<Response>` / `ctx.json(anything)`) are never type-checked against the operation response model; CI contract checks are non-blocking; ~31% of ops have no Hurl scenario. Symptom of raw-handler-test-blindspot. | (1) PRIMARY: flip `contract.yml` Hurl + Schemathesis to `continue-on-error: false` (TODO already at :70) so Schemathesis `response_schema_conformance` catches all 357 schema-carrying ops. (2) Bind registry handler return types to generated response models (`satisfies`) so drift fails `typecheck`. Do NOT hand-re-pin 369 ops in Hurl. |
| **phi-redaction-jsonb** (Pino redact paths don't descend into nested JSONB PHI; related: no "no-PHI-in-notification-content" guarantee) | dental-patient (active fix), dental-audit (verified-clean, honors guard), notifications (a11y-label PHI leak fixed; producer spot-check clean) | P2 | V1 RECOMMENDED | INPUT B: `logger.ts:14-23` PHI_REDACT_PATHS = bare keys + `*.key` (one level only); `logger.test.ts:40` is the ONLY nesting test (2-deep); `updateDentalPatient.ts:135-138` in-repo admission Pino "does not descend into" emergencyContact/communicationPreferences. Real 3-deep shapes: `patient.emergencyContact.{name,phone,email}`, `person.contactInfo.{email,phone}`. Audit full-row path is already safe via `audit-phi-sanitizer.ts` (recursive). | Single redact config, but `*.field` matches one level only; no leaf-key deep matching; no lint guard. **Latent today** (no call site logs a whole patient/person object — grep of ~769 calls found none), but one future `logger.info({patient})` leaks cleartext PHI. Separately, notifications closed its per-row aria-label PHI instance and spot-checked producers clean, but the platform "no PHI in notification title/message content" guarantee remains **convention-only** (no lint/test). | Single-point fix in `core/logger.ts`: replace static path list with a recursive `censor`/formatter reusing the proven `audit-phi-sanitizer.ts` pattern; add missing leaf keys (emergencyContact, communicationPreferences, primaryAddress, contactInfo, consent); add 3-deep regression tests + a `no-restricted-syntax` lint banning `logger.*({ patient/person/...spread })`. Pair with a small "no-PHI-in-notification-content" convention/test for the notifications producers. |
| **optional-branchid-tenant-leak** (EM-BIL-002: omitted branchId scopes report to caller, not whole tenant) + handler-trusts-body-tenancy residual | dental-billing (fixed+pinned), data-governance/erasure (same class, partially hardened) | P3 (residual) | V1 RECOMMENDED (guard) | INPUT B: all 5 billing report handlers carry the fix (`getArAging.ts:38-43` else `getActiveBranchIdsForPerson`); empty-set → `sql\`false\``; 5-endpoint two-tenant regression test exists; `SWEEP_optional-branchid_2026-06-08.md` swept 45+ endpoints clean. **Erasure residual (two halves):** (a) list/get/approve still admin-wide with optional tenantId (omit → all tenants); (b) `requestErasure` still falls back to caller-supplied **body** tenantId for bare-person/unresolvable subjects (`erasure-service.ts:64-65,96-98`) — i.e. handlers still trust tenancy from the request body. | Runtime guard exists (facade fn + zero-row default) but is **convention-only** — grep of all eslint configs found NO rule referencing branchId/tenant/getActiveBranchIdsForPerson; guard lives in a dental-org facade, hand-applied. A new aggregate/report endpoint could re-introduce the leak with no CI signal; erasure handlers still accept tenancy from the body. | (1) ESLint/contract-spine rule flagging handlers reading optional branchId that reach a repo SELECT without routing through `getActiveBranchIdsForPerson`/throwing. (2) Promote `getActiveBranchIdsForPerson` into `handlers/shared/`. (3) Add EM-BIL-002 to module-review checklist. (4) Erasure body-tenancy fallback is decision-gated — see §9/§13. Do NOT retrofit frozen single-tenant base handlers. |
| **openapi-null-drop** (openapi-ts@0.97 drops `\| null` on nullable string-enum fields) | case-presentation, dental-perio (FE `?? null` workarounds), + 4 unguarded latent fields | P2 | V1 RECOMMENDED | INPUT B: 14 nullable string-enum fields lose `\| null` spec-wide; `types.gen.ts:5675 decision: 'accepted'\|'rejected'` (no null) while `:5679 decisionAt: Date\|null` is preserved (drop is enum-specific). Runtime null is REAL: `case-presentation.schema.ts:53` decision null until accept/reject. FE comment `use-case-presentation.ts:84` "the SDK enum omits the null variant". 4 fields (angleClass, calibrationMethod, fromStatus, eventType) have NO guard. | Generator-layer bug in openapi-ts on OpenAPI 3.0 `nullable:true`+inline enum; per-call-site `?? null` masks it; no lint/codegen/ADR guard. | Emit OpenAPI 3.1 from TypeSpec emitter (`openapi-versions: ['3.1.0']`) — 3.1 nullability `type:['string','null']` survives openapi-ts; OR upgrade openapi-ts past 0.97. Then delete scattered `?? null`. Add a tiny post-regen assertion that `CasePresentation.decision` retains `\| null`. |
| **rbac-role-truth-drift** (TypeSpec declares `["user"]` while handler enforces admin; ROLE_PERMISSION_MATRIX internally inconsistent) | data-governance (8 erasure/legal-hold ops), dental-audit (matrix lines 100 vs 183), dental-org (matrix NEEDS-REVIEW), dental-billing/dental-clinical/dental-visit (FE-affordance-vs-backend) | P2 | V1 RECOMMENDED | INPUT B: 230/362 routes declare `["user"]`; 218/220 dental ops `["user"]` is intentional coarse gating; exactly 8 erasure/legal-hold ops declare `["user"]` but handler enforces `user.role !== 'admin'`. Runtime is correct (dental-erasure.hurl Scenario 8 asserts 403) — drift is contract/SDK-truth. ROLE_PERMISSION_MATRIX flagged NEEDS-REVIEW (auditor row 100 vs 183). | No central RBAC registry; each TypeSpec hand-rolls per-op role arrays; route generator copies declared roles verbatim with no handler cross-check. | (1) Change 8 declarations `["user"]→["admin"]` in dental-erasure.tsp (5) + dental-legal-hold.tsp (3), regen. (2) Resolve ROLE_PERMISSION_MATRIX auditor-row contradiction (product decision). (3) Optional spec-vs-handler RBAC consistency check + canonical system-role constant. Do NOT mass-rewrite the 218 intentional `["user"]` ops. |
| **audit-event-parity-gap** (a second write path to an auditable/state-changing resource skips the audit log + domain event the canonical path writes; or computed clinical record stored only in audit metadata) | dental-scheduling (PATCH-cancel skipped AL-008/DE-011 — fixed), dental-perio (diagnosis only in audit metadata — fixed), dental-audit (3 sinks, base PHI-reads invisible to viewer — open), dental-imaging (logAuditEvent calls lack row pins — open), dental-billing (verified strong, not a gap) | P2 | V1 RECOMMENDED (guard) | INPUT A: scheduling fix-report "PATCH-cancel skipped the audit log + domain event the DELETE path writes (unaudited cancellations)"; perio gap-plan §12 "Audit-metadata diagnosis blob as only persistence"; audit gap-plan §5 GAP-3 "3 audit tables; base-module PHI-access reads invisible to dental viewer". | No shared guard ensures every handler mutating an auditable state emits the same audit+event regardless of verb; audit sink fragmentation (3 sinks) means the trail is not a single pane. Per-module fixes close symptoms only. Only **2 active-open** instances (audit, imaging) — scheduling + perio already fixed+pinned. | (1) Targeted (NOT repo-wide) artifact: keep the appointment-cancel verb-parity pins (DELETE+PATCH, done) and add a sink-divergence canary (audit FIX-004) as a real-Postgres test asserting authoritative vs legacy counts. (2) DEFER any generalized verb-parity lint/framework until a 3rd independent instance appears (same "wait for the next instance" discipline used in §7). Treat sink consolidation as a separate decision-gated effort. |
| **scheduler-guardrail** (single shared `core/jobs.ts` scheduler; "no scheduler exists" was a propagated erratum) | dental-billing, dental-visit, dental-scheduling, notifications, data-governance, dental-clinical, dental-patient, dental-pmd, dental-org, dental-imaging, dental-perio (all reference it) | P3 | V1 RECOMMENDED (guard, low cost) | INPUT B: `isRealIssue:false` runtime — every setInterval is inside `jobs.ts`; zero scheduler-lib imports outside it; `createJobScheduler` called once (`app.ts:84`), 8 register*Jobs across 7 modules. Billing status-sweep "no new infrastructure". Erratum corrected billing + dental-visit "no scheduler" gap-plan claims. | Runtime pattern uniform and correct; guard is convention-only — no ESLint rule bans `setInterval`/cron-lib imports outside `jobs.ts`; no single-scheduler invariant test. | Cheap hardening only: `no-restricted-syntax`/`no-restricted-globals` banning bare setInterval/cron-lib imports in `src/handlers/**` (exempt `core/jobs.ts`); optional one-file invariant test. NOT a defect — DO NOT build any new scheduler. |
| **fix-ready-erratum-understates-drift** (meta-pattern: a fix-ready plan asserts "backend exists, FE-wiring-only / no drift" which the fix pass proves false) | dental-org (consent P2→P1), dental-billing ("no scheduler exists" erratum; also receipt "backend ready" → flat→nested drift), dental-patient (no staff demographics path at all), dental-perio (2 completion-response drifts), dental-scheduling ("no shared files" → found unaudited PATCH-cancel), dental-visit (scheduler erratum) | P2 | V1 RECOMMENDED (process) | INPUT A: dental-org B3 "The plan's erratum said... This was incorrect"; patient headline "framed FIX-001 as FE-only wiring... revealed this is false — there was no staff backend path at all"; scheduling §2 "Shared/platform work: none" contradicted by adversarial review. | Fix-ready plans trust gap-plan/erratum claims without verifying SDK-type-vs-handler-shape before wiring; the same orchestrator warning ("verify before wiring") had to be re-learned per module. | Process guardrail, not code: make "verify SDK type vs handler shape (and contract vs handler) BEFORE wiring; treat fix-ready 'backend ready' as unverified" a mandatory step in `04-module-or-group-fix-tdd.md`. This is the same incident-chain as raw-handler-test-blindspot + apiok-contract-drift. |
| **summary-body-coherence** (summary total/count computed from a different source than the rendered body) | case-presentation (viewer grand-total — fixed via DOM oracle), notifications (badge vs rows — immune by construction) | P3 | V2 DEFERRED | INPUT A: case-pres fix-report "added a DOM-derived `assertTotalExplainedByRows` coherence guard"; notifications "badge=pagination.totalCount, rows=data from same query... structurally immune". | Per-module FE wiring; shared `assertTotalExplainedByRows` oracle already exists in test-utils and was reused. No new shared work needed. | None platform-level required — the shared DOM-coherence oracle already exists and is the right tool. Encourage reuse via FE testing docs; do not build a new abstraction. |
| **dead-trigger / orphan-affordance** (backend-complete + tested + RBAC-pinned ops unreachable from FE; shared top-bar dead props) | dental-clinical (onLab/onPmd — fixed render), dental-pmd (onPmd rendered by clinical Batch A this sweep; PMD's own deeper batches remain), dental-scheduling (7 of 19 orphan lifecycle ops), notifications (write-only subsystem, 4 read ops 0 consumers), dental-org (consent-template CRUD 0-FE), dental-billing/dental-visit (orphan cluster) | P1 | V1 RECOMMENDED (guard) | INPUT A: clinical fix-ready §12 "Dead-trigger class... flagged for prompt 05"; scheduling gap-plan §16 "7 orphans = lifecycle-affordance cluster... identical to billing/clinical"; notifications gap-plan §16 "write-only subsystem". `workspace-top-bar.tsx` touched by both clinical (onLab) AND pmd (onPmd). | No test/lint convention forcing prop→rendered-affordance parity; no affordance-existence smoke harness; "backend-first build, FE render never added" pattern recurs. Per-module renders fix instances; root cause unfixed. | Lint/test convention asserting shared-shell props map to rendered affordances; a reusable affordance-existence assertion in test-utils. Decision-gated which orphans to wire vs declare (do not wire decision-blocked ops). |

## 4. Shared Files / Services With High Blast Radius

| File/Service/Component | Main Consumers | Risk | Evidence | Recommendation |
|------------------------|----------------|------|----------|----------------|
| `services/api-ts/src/core/audit-logger.ts` (`logAuditEvent`) | ~98 callers across 15 modules; the single PHI-sanitize + fail-closed write choke point | Any signature change = platform-wide blast radius; it is upstream of audit-event-parity + phi-redaction | dental-audit gap-plan §16/§21 KG (widest fan-in); INPUT B `safeAuditLogFields:108-119` + `audit-phi-sanitizer.ts` recursive | FROZEN — no signature change. Add the legacy-sink divergence canary (FIX-004) as a real-Postgres test; do NOT migrate sinks big-bang. |
| `services/api-ts/src/core/jobs.ts` (`createJobScheduler`/`registerCron`) | 8 register*Jobs across 7 modules; ~43 KG refs; instantiated once in `app.ts:84` | Sole scheduler — but uniform/correct; only risk is a future ad-hoc scheduler bypassing it | INPUT B single-scheduler investigation (`isRealIssue:false`); billing/visit/scheduling/notifs all register here | Add cheap lint guard banning setInterval/cron-lib outside jobs.ts. Otherwise leave as-is. |
| `services/api-ts/src/core/logger.ts` (`PHI_REDACT_PATHS`/`createLogger`) | Single factory, instantiated once (`app.ts:79`), feeds 100% of ~769 logger call sites / 210 handler files | Latent cleartext-PHI leak if any future site logs a whole JSONB-bearing object | INPUT B pino investigation (one-level-only redaction; `logger.test.ts:40` sole nesting test) | Harden the single redact config (recursive censor) + lint guard. Single-point fix covers all sites. |
| `services/api-ts/src/generated/openapi/routes.ts` + `validators.ts` + `registry.ts` | All 369 ops; 505 handler files; consumed by SDK + FE | 0 response validators wired (62 generated, all dead); untyped registry literal → envelope drift compiles green | INPUT B apiok investigation (`routes.ts` 513 input validators, 0 response; `registry.ts:376` plain literal) | Bind registry return types to response models (`satisfies`); wire response validation in dev/test. Generated — never hand-edit. |
| `apps/dentalemon/src/features/case-presentation/accepted-plan-viewer.tsx` (NEW shared signed-acceptance/plan-version viewer) | case-presentation (owner), dental-visit (GAP-3 consumption-pending) | Cross-module bundle — dental-visit's GAP-3 is satisfied-pending-wiring on this landing | case-pres fix-report FIX-002; dental-visit gap-plan GAP-3 | Treat dental-visit GAP-3 as satisfied-pending-wiring. Do NOT build a duplicate viewer. |
| `apps/dentalemon/src/components/print/printable-document.tsx` (NEW shared print/PDF primitive) | dental-billing (receipt, owner/first consumer); declared future: dental-patient statement, case-presentation estimate | 3 declared consumers — risk is a SECOND print primitive being built | dental-billing fix-report E2/E4; patient + case-pres "[DO NOT OVERBUILD]" forbiddings | Single print primitive. dental-patient + case-presentation MUST consume `layout='a4'`, not rebuild. |
| `apps/dentalemon/src/features/settings/settings-panels.tsx` (NEW shared panel registry) | dental-org (owner); planned mounts: data-governance retention panel, dental-pmd cert panel, dental-audit (already mounted) | Shared settings seam — modules must mount onto registry, not build standalone shells | dental-org fix-report B4/B8; audit fix-report (appended 'audit' entry); data-gov/pmd "[CROSS-MODULE RISK]" | Canonical mount point. data-governance/pmd/clinical-consent-template panels mount here; forbid local settings shells. |
| `apps/dentalemon/src/features/workspace/components/workspace-top-bar.tsx` (shared workspace shell) | dental-clinical (onLab), dental-pmd (onPmd), dental-visit (rbac helpers) | High-traffic dead-prop seam — two modules' affordances, no prop→render parity guard | dental-clinical gap-plan §21; dental-pmd gap-plan §16 (dead-prop ×2) | Single coordinated fix point. Add prop→affordance parity guard (see §3 dead-trigger). |
| `services/api-ts/src/handlers/dental-visit/visits/updateDentalVisit.ts` (visit-completion hot path) | dental-pmd (auto-gen trigger, dynamic import), dental-clinical/billing (completion audit) | Cross-module side-effect insertion point — other modules will reuse this seam | dental-pmd fix-report §4/§8 (failure-isolated, idempotent trigger) | Keep failure-isolated + idempotent. Pin failure-isolation tests on any new completion side-effect. |
| `apps/dentalemon/src/test-setup.ts` (lucide-react mock allowlist) | All FE component tests rendering icons; 38 files import lucide, ~55 icons | Hand-maintained 55-entry allowlist; 5 used icons already silently missing (latent) | INPUT B test-infra investigation (BarChart3, ClipboardList, Home, Settings, UserCog unlisted) | Replace with Proxy/auto-stub (or generate from lucide export keys); else add a coverage assertion. |

## 5. Platform-Level Fix Candidates

| Fix | Why Platform-Level | Modules Helped | Severity Addressed | Test Requirements | Risk |
|-----|--------------------|----------------|--------------------|--------------------|------|
| Shared `buildTestApp` helper that mounts handlers WITH generated validators + lint guard against raw-handler mounts | 307 tests each roll their own harness; 203 skip the validator entirely — the mechanism that ships every contract drift | All 11 modules citing raw-handler-test-blindspot; especially dental-imaging (17 files, 0 validators) | P1 | New helper must reproduce prod auth+zValidator chain; migrate dental-imaging first; RED a known drift (BUG-IMG-001) under the new helper | Medium — touches many test files; migrate incrementally, keep Hurl as backstop |
| Flip `contract.yml` Hurl + Schemathesis to `continue-on-error: false` | One CI-file change gates response-schema conformance across all 357 schema-carrying ops; catches envelope drift without per-op pins | All 6 apiok-contract-drift modules + future ops | P2 | Clear the pre-existing-failures backlog first (8 env-dep failures: MinIO/Mailpit/Stripe); confirm Schemathesis green | Medium — flipping before backlog cleared blocks merges; sequence after env baseline fixed |
| Harden `core/logger.ts` redaction to recurse JSONB + lint guard against logging whole PHI objects | Single redact config feeds 100% of logging; reuses proven `audit-phi-sanitizer.ts` recursive pattern | dental-patient, dental-audit, notifications, and every PHI-handling module (latent) | P2 (latent P1 if a future site logs a whole object) | 3-deep redaction tests for real shapes ({patient:{emergencyContact:{phone}}}, {person:{contactInfo:{email}}}); lint rule test | Low — additive; keep audit-path sanitizer as-is |
| Emit OpenAPI 3.1 (or upgrade openapi-ts) so nullable string-enums retain `\| null` + post-regen assertion | One generator-config change recovers `\| null` on all 14 fields, removes scattered `?? null` workarounds | case-presentation, dental-perio, + 4 unguarded fields (angleClass/calibrationMethod/fromStatus/eventType) | P2 | Post-regen type/grep assertion that `CasePresentation.decision` includes `\| null`; remove redundant FE coercions | Medium — 3.1 emission may ripple SDK types; verify FE typecheck green after regen |
| ESLint/spine guard for optional-branchId → caller-scope + promote `getActiveBranchIdsForPerson` to `handlers/shared/` | Runtime fix exists but is convention-only; a new aggregate/report endpoint could re-leak with no CI signal | dental-billing (done), future cross-resource report endpoints platform-wide | P3 | Lint rule unit test; promote-and-reimport smoke | Low — prevention-only, no runtime change |

## 6. Issues That Should Stay Module-Local

| Issue | Module/Group | Why Not Platform-Level | Recommendation |
|-------|--------------|------------------------|----------------|
| `updateTreatmentPlan` PATCH back-door to `approved` without a `createApproval`/consent record | dental-patient (treatment-plan FSM) | FSM entirely owned by dental-patient; only one of 3 paths drifts; billing's "approved" is a separate insurance-claim FSM | Remove `approved` from generic-PATCH-reachable transitions OR require the PATCH branch to write a `createApproval` row; add a parity test (every `approved` plan has an approval row) |
| Lab-status enum doc drift (API_CONTRACTS `sent/received/...` vs code `ordered/in_fabrication/...`) + 5MB vs 50MB attachment cap | dental-clinical | Doc-vs-code drift local to one module's spec docs; no shared mechanism | Reconcile docs to code in dental-clinical Batch F (doc-only) |
| Dead contact-info UI (profile phone/email rows always "—"; `getDentalPatient` excludes contactInfo per V-PAT-014) | dental-patient | Product-coherence decision local to patient profile | [NEEDS PRODUCT DECISION] — suggested default pending decision: remove the dead rows for V1 (do not execute before the decision) |
| `authorMemberId` on a generated PMD = the completing actor, not necessarily the assigned/treating dentist; this identity is folded into the document checksum | dental-pmd | Identity-attribution local to PMD generation; coupled to the signature-honesty (FIX-005) decision | [NEEDS PRODUCT DECISION] — confirm whether the PMD author/attestor must be the treating dentist (affects the checksummed identity). Also noted in §11 (schema-identity, note-only). |
| `recordedByMemberId` client-supplied/forgeable payment attribution | dental-billing | Attribution-integrity local to payment recording; blocked on derive-vs-validate decision | [NEEDS PRODUCT DECISION] (GAP-6); decide then derive from session |
| Signature non-repudiation "theater" (SHA-256 hash claimed as digital signature; `sign()` 0 callers) | dental-pmd | Copy/claims-honesty local to one module's generatePMD; Q2-blocked | [NEEDS PRODUCT DECISION] (sign-or-strip, FIX-005) |
| BR-005 auto-discard flag-ON path untested (env flag default OFF) | dental-visit | Env-flag-gated single code path | Add a flag-ON test in dental-visit when the flag is productized |

## 7. Do Not Centralize Yet

| Candidate | Label | Why Deferred or Rejected |
|-----------|-------|--------------------------|
| Shared summary-body coherence oracle abstraction | [DO NOT OVERBUILD] | The `assertTotalExplainedByRows` DOM oracle already exists in test-utils and is reused; building anything more is over-engineering for a pattern that appears in 2 modules. |
| Audit-sink consolidation (3 sinks → 1) | V2 DEFERRED / [NEEDS PRODUCT DECISION] | Big-bang sink migration is explicitly forbidden; the parity gap is real but consolidation needs a Q2/Q3 legacy-sunset decision. Start with a divergence canary, not consolidation. |
| Shared "staff-edits-Person" primitive | [DO NOT OVERBUILD] | Only dental-patient needed it so far (one facade). `updatePerson`'s self-edit lock is upstream account-template; wait for a 2nd module to need it before abstracting. |
| New affordance-existence smoke harness for every module | V2 DEFERRED | Real gap (dead-trigger), but a reusable test-util assertion is cheaper than a per-module smoke framework; defer the framework, add the assertion. |
| Per-op Hurl response-shape pins for all 369 ops | DO NOT ADD | This is the brittle status quo that let 3 drifts through; Schemathesis conformance (§5) is the right layer, not 369 hand-written assertions. |
| Real event bus / async pipeline for audit/notifs | DO NOT ADD | ADR-005/006 settled inline-sync; any scheduled work registers on `core/jobs.ts`. |
| Central RBAC permission registry/role-matrix engine | V2 DEFERRED | Worth considering, but the immediate need is correcting 8 declarations + resolving the matrix contradiction; a full registry engine is overbuild until the matrix-truth decision lands. |

## 8. Test Infrastructure Findings

| Finding | Evidence | Affected Modules | Recommended Fix | Priority P0-P3 |
|---------|----------|------------------|-----------------|----------------|
| **Raw-handler tests skip the generated validator** — the mechanism that lets request-shape contract drift (BUG-IMG-001, consent-template, create-template) ship | INPUT B: 203/307 handler tests raw-only; all 17 dental-imaging files 0 validators; `dental-imaging.hurl` case 20b comment; no shared `buildTestApp` | All modules; dental-imaging worst | Shared validator-mounting `buildTestApp` + lint guard (see §5) | P1 |
| **CI contract checks non-blocking** — Hurl + Schemathesis both `continue-on-error: true`, so envelope/shape drift cannot fail a merge | INPUT B: `contract.yml:123-129`; explicit "Flip... once backlog cleared" TODO at :70 | All ops (357 schema-carrying) | Clear env-dep backlog, then flip to blocking | P1 |
| **Status-only negative-path assertions** — kill-switch/feature-disabled tests assert 403 only, not the data-safety invariant (zero rows written) | INPUT A imaging FIX-001: "asserted only 403... never that zero landmark rows were written"; receipt hurl HTTP-200-only | dental-imaging, dental-billing, dental-scheduling | Assert side-effect absence, not just status; pin nested response shape | P2 |
| **Retries-disabled test client masks production retry behavior** — `freshClientWithMutations` hid the imaging 403 retry-storm | INPUT A imaging FIX-002; fix added `prodLikeMutationClient` | dental-imaging (+ any mutation tested only retries-off) | Test critical mutations under a prod-like retry client | P2 |
| **`bun test <directory>` footgun** — a directory arg bypasses the per-file DB clone → all files run in ONE clone → mass false TRUNCATE failures | INPUT B: `test-with-db.ts:89-90` (non-glob positional → explicitFiles → `bun test <dir>`); `MASTER-GAP-MATRIX.md:289` "never bun test <path>"; billing whole-dir false failures | All backend modules (101 TRUNCATE files) | Expand directory args via Bun.Glob to per-file, or reject directory args with a clear error | P2 |
| **Hand-maintained lucide mock allowlist** — each new icon must be added or component tests fail; 5 icons already silently missing | INPUT B: `test-setup.ts:37-100`; INPUT A clinical (FlaskConical/IdCard), notifications (Bell) | dental-clinical, notifications, all FE-icon modules | Proxy/auto-stub from lucide export keys, or a coverage assertion | P3 |
| **Contract suite env-dep failures** — 8 standing failures (MinIO/Mailpit/Stripe) reviewers must distinguish from real drift; stale `:7213` server masks drift | INPUT A dental-visit verification; INPUT B `run-contract-tests.ts` deps | All modules running contract suite | Surface env baseline + "restart server before contract" as runner preflight warnings | P3 |
| **logAuditEvent call sites lack persisted-row assertions** — tests verify invocation, not that an audit row persists with correct actor/entity | INPUT A imaging FIX-004; data-gov enforced-mode retention; audit FIX-004 | dental-imaging, data-governance, dental-audit | Add row-written assertions (real-Postgres harness) at high-value audit sites | P3 |

## 9. Domain / Workflow Pattern Findings

| Finding | Affected Modules/Groups | Evidence | Risk | Recommendation |
|---------|-------------------------|----------|------|----------------|
| Cross-module FSM ownership mostly reconciled; one residual back-door | dental-patient (treatment-plan accept), dental-visit, case-presentation | INPUT B: `acceptCasePresentation.ts:81-86` + `approveTreatmentPlan` converged; `updateTreatmentPlan.ts:80` PATCH reaches `approved` writing only `approvedAt`, no approval/consent record | P2 — a plan can be "approved" without the non-repudiation artifact the other 2 paths require | Module-local: close the PATCH back-door + parity test (see §6) |
| Dual write-path to a cancellable state, only one audited (now reconciled+guarded) | dental-scheduling | INPUT B: DELETE `cancelAppointment` + PATCH `updateAppointment{status:cancelled}` now both write AL-008/DE-011, identical role/reason; `dental-scheduling-transitions.test.ts:370` RED→GREEN pins | Resolved for scheduling; class unguarded platform-wide | Platform convention/test for verb-path audit parity (§3 audit-event-parity-gap) |
| Visit-completion → PMD auto-gen side-effect on a shared hot path (reconciled+guarded) | dental-pmd, dental-visit | INPUT B: `updateDentalVisit.ts:168-185` failure-isolated + idempotent; `pmd-generation-trigger.failure-isolation.test.ts` | Low — single shared core, two callers, pinned | None — already mitigated; reuse pattern for future completion side-effects |
| Erasure tenant-scope + data-controller authority is the one OPEN workflow decision | data-governance (erasure) | INPUT B: `listErasureRequestsHandler.ts` optional tenantId → all tenants; `approveErasureHandler` by-id no tenant scope; bare-person subjects fall back to caller tenantId; clinic dentist_owner cannot erase own patients | P2 — attribution/disclosure-integrity (anonymize is keyed on subjectPersonId, not tenant, so NOT wrong-patient deletion) | [NEEDS PRODUCT DECISION] Q1 (who may erase) + Q2 (tenant-scope list/get/approve); then shared fix reusing the FIX-001 facade |
| Write-only subsystems with producer fan-in but zero FE consumers | notifications (in-app gap closed Batch A; push opt-in open), dental-scheduling (7 orphan lifecycle ops), dental-org (consent-template CRUD) | INPUT A notifications gap-plan §16; scheduling gap-plan §16 | P1 — complete audited backends are undeliverable from the product | Decision-gated wire-or-declare per orphan; see §3 dead-trigger |

## 10. Webwright / Playwright Findings

Webwright and Playwright/E2E were **NOT run** for this audit — the evidence directory was empty and no browser/server was booted. E2E spec **files** were read statically (e.g. `lab-order-tracking-api.spec.ts`, `imaging-cbct.spec.ts`, journey specs) to assess test-honesty, and those static observations are folded into §3 (dead-trigger, raw-handler-test-blindspot) and §8 (status-only assertions, harness-route E2E masquerading as production coverage). No runtime/visual findings are reported here. Any cross-cutting finding that asserts production reachability (e.g. dead-affordance fixes, CBCT finalize chain, offline-conflict E2E) remains runtime-unverified and is flagged as such in §13 [BLOCKED BY ENVIRONMENT].

## 11. Database / Schema-Related Cross-Cutting Findings

| Finding | Affected Modules/Groups | Evidence | Should Go To 06-database-schema-audit.md? | Recommendation |
|---------|-------------------------|----------|-------------------------------------------|----------------|
| No unique index on `pmd_document.visit_id` — concurrent double-completion can race to two `generated` rows (now reachable because the trigger is on the hot path) | dental-pmd | INPUT A dental-pmd notes (flagged for prompt-06, [NEEDS CONFIRMATION]); INPUT B idempotency = supersede-on-read | Yes | Prompt 06: evaluate a unique index / upsert on `pmd_document.visit_id` to make the supersede race impossible at the DB layer |
| `safety_floor_merged` is a text-typed boolean (permanently text `'false'`) | dental-pmd | INPUT A dental-pmd notes ([NEEDS CONFIRMATION]; optional text→boolean migration only if folded into Batch C) | Yes | Prompt 06: flag text-typed-boolean; fold any migration into Batch C, not standalone |
| New nullable diagnosis columns (`stage`/`grade`/`extent` text + `risk_factors` jsonb on `dental_perio_chart`, migration 0101) — added by Batch A; relate to openapi-null-drop on the read path | dental-perio | INPUT A dental-perio (migration 0101_complete_junta.sql, 4 additive nullable cols); INPUT B openapi-null-drop on PerioChart.stage/grade/extent | Yes (consistency check only) | Prompt 06: verify nullable column ↔ contract nullability consistency; no schema change implied here |
| JSONB-PHI columns relevant to logger redaction (`patient.emergencyContact/communicationPreferences/followUpNotes`, `person.contactInfo/primaryAddress/consent`) | dental-patient, person | INPUT B pino investigation (`patient.schema.ts:40/136/144`, `person.schema.ts:31/32/38`) | Yes (inventory only) | Prompt 06: inventory JSONB columns carrying PHI so the §5 logger hardening and any at-rest considerations are scoped against ground truth |
| Erasure `tenantId` is metadata-only; anonymize keyed solely on `subjectPersonId` across person/patient/clinical/imaging/storage facades | data-governance | INPUT B erasure investigation | Note only (not a defect) | Prompt 06: confirm anonymize-target coverage; mitigating factor that this is attribution-integrity, not wrong-patient deletion |
| `pmd_document.authorMemberId` identity = completing actor vs assigned/treating dentist (folded into the document checksum) | dental-pmd | INPUT A dental-pmd notes [NEEDS PRODUCT DECISION] | Note only — schema-identity, not a schema-shape change | Prompt 06: note-only; the actual decision is tracked in §6 (module-local) + §13 (blockers), not a migration |

This section flags repeated data/schema items for prompt 06 only — no full schema audit was performed here.

## 12. Recommended Platform Fix Order

| Order | Fix | Why Now | Modules Helped | Tests Needed First | Risk | Recommended Prompt |
|-------|-----|---------|----------------|--------------------|------|--------------------|
| 1 | Shared validator-mounting `buildTestApp` helper + raw-handler lint guard | Highest risk-reduction: it is the mechanism that ships every apiok-contract-drift; closes the detection hole that hid BUG-IMG-001, consent-template, create-template | All 11 raw-handler modules; dental-imaging first | RED a known drift under the new helper before migrating | Medium — **V1 RECOMMENDED**, incremental migration, Hurl remains the backstop (not a required big-bang) | future specialized platform fix prompt (test-harness; too cross-cutting for one module batch) |
| 2 | Harden `core/logger.ts` JSONB-recursive redaction + lint guard | PHI safety; single-point fix; latent P1 if any future site logs a whole object | dental-patient, dental-audit, notifications, all PHI modules | 3-deep redaction regression tests for real shapes | Low | future specialized platform fix prompt (platform logger/security) |
| 3 | Flip `contract.yml` Hurl + Schemathesis to blocking | Makes response-schema conformance gate all 357 ops; locks in the apiok-drift class platform-wide | All apiok-drift modules + future ops | Clear 8 env-dep failures (MinIO/Mailpit/Stripe) backlog first | Medium — **V1 RECOMMENDED**, gated on env backlog (not blocking until cleared) | future specialized platform fix prompt (CI) |
| 4 | RBAC: correct 8 erasure/legal-hold `["user"]→["admin"]` declarations + matrix-contradiction decision | Contract/SDK-truth drift on the most sensitive (erasure) ops; runtime already safe but advertised role is wrong | data-governance, dental-audit, dental-org | dental-erasure.hurl Scenario 8 still 403 after regen | Low | 04-module-or-group-fix-tdd.md (data-governance Batch C; module-batchable) |
| 5 | OpenAPI 3.1 emission (or openapi-ts upgrade) + post-regen null assertion | Removes scattered `?? null`; fixes 4 unguarded latent fields trusting wrong non-null types | case-presentation, dental-perio, +4 fields | Post-regen assertion `CasePresentation.decision` retains `\| null`; FE typecheck green | Medium | future specialized platform fix prompt (codegen) |
| 6 | Optional-branchId recurrence lint guard + promote `getActiveBranchIdsForPerson` to `handlers/shared/` | Prevention-only; runtime already fixed; protects future report endpoints | dental-billing (done), future modules | Lint rule unit test | Low | 04-module-or-group-fix-tdd.md (billing Batch D + shared-promote) |
| 7 | Single-scheduler lint guard (ban setInterval/cron-lib outside jobs.ts) | Cheap hardening; pattern correct but unenforced | All job-owning modules | One-file invariant test | Low | 04-module-or-group-fix-tdd.md (low-priority hardening) |
| 8 | Targeted audit verb-parity pins + legacy-sink divergence canary (NOT a repo-wide verb-parity framework) | Closes the "second write path skips audit" class for the 2 active-open instances; defer a generalized framework until a 3rd instance | dental-scheduling (done), dental-audit, dental-imaging | Real-Postgres count-parity canary | Low–Medium | 04-module-or-group-fix-tdd.md (dental-audit Batch C/FIX-004) |

**Ordering rationale (all of orders 1–8 are `V1 RECOMMENDED`, none are `V1 REQUIRED`):** order 1 (shared `buildTestApp` harness) is ranked first because it shifts contract-drift detection *left* to the cheaper, faster unit layer and is the common root of the apiok-drift class — accepting a knowingly higher build cost, mitigated by incremental migration with Hurl as the backstop. The lower-code `contract.yml` blocking flip (order 3) is ranked behind it only because it is **gated on clearing the 8-failure env backlog**; if that backlog is cleared first, order 3 becomes the cheapest immediate win and may be pulled forward. Neither is a required big-bang.

## 13. Dependencies / Blockers

| Blocker | Label | Why It Matters | Suggested Next Step |
|---------|-------|----------------|---------------------|
| Who may erase (platform-admin vs clinic dentist_owner) + erasure list/get/approve tenant-scope | [NEEDS PRODUCT DECISION] | Blocks data-governance admin UI, final role names, and closing the cross-tenant PII list-leak (ER-P1-1); clinic data-controller currently cannot erase own patients (GDPR/RA-10173) | Resolve Q1/Q2; then shared fix reusing the FIX-001 tenant-resolution facade |
| Voice-charting / no-AI clinical-trust stance | [NEEDS PRODUCT DECISION] | ONE product conversation spans BOTH dental-perio (voice-charting MODULE_SPEC vs STANDARDS_COMPLIANCE non-goal) AND dental-imaging (FakeDetector auto-detect vs no-AI stance) — synthesizer dedupes these into a single decision | Decide the platform no-AI/voice stance once; then resolve perio Q1 + imaging Q1/FIX-007 together |
| Claims/insurance vertical (billing GAP-7 == dental-patient GAP-3, ~10 patient ops) | [CROSS-MODULE RISK] / [NEEDS PRODUCT DECISION] | Single decision OWNED BY dental-billing; backend 100% built but Phase-2; FE half-shipped worklist with no create path = misleading surface | Decide in billing alone; do not wire in patient independently |
| dental-pmd remaining batches + cert custodian panel coupling (reachability already satisfied this sweep) | [CROSS-MODULE RISK] | dental-clinical Batch A **already rendered the `onPmd` top-bar button** (commit d774c1e5; satisfies pmd GAP-2), so the PMD document chain is reachable now. What remains is PMD's OWN Batch B/C/D/E (content / safety-floor merge / honest E2E / signing); and the cert custodian panel must mount on dental-org's shared settings shell, never a parallel shell | Sequence pmd Batch B→E on its own; mount the cert panel on the settings-panel registry (do not build a parallel shell) |
| Contract CI cannot be flipped blocking until env-dep backlog cleared | [BLOCKED BY ENVIRONMENT] | 8 standing failures (MinIO/Mailpit/Stripe) would block all merges if flipped now | Provision/stabilize env baseline, clear backlog, then flip `continue-on-error:false` |
| No runtime/browser verification this pass (Webwright/Playwright not run) | [BLOCKED BY ENVIRONMENT] | Dead-affordance fixes, CBCT finalize chain, offline-conflict promise remain runtime-unverified | Schedule a runtime sweep (full stack + MinIO + reseed) after the test-harness fix lands |
| KG/contract-spine dual-operationId false-orphan inflation | [NEEDS CONFIRMATION] | Inflates imaging orphan counts; every orphan claim had to be grep-verified | KG-refresh backlog item (V2 platform tooling) |

## 14. Deferred / Do Not Build

| Item | Label | Why Not Active |
|------|-------|----------------|
| Audit-sink consolidation (3 sinks → 1) | V2 DEFERRED | Big-bang migration forbidden; needs Q2/Q3 legacy-sunset decision; start with divergence canary |
| New event bus / async pipeline | DO NOT ADD | ADR-005/006 settled inline-sync; scheduled work registers on `core/jobs.ts` |
| Any new scheduler/cron framework | DO NOT ADD | `core/jobs.ts` exists and is uniformly consumed; "no scheduler" was a propagated erratum |
| Second print/PDF utility (in dental-patient, case-presentation, or anywhere) | [DO NOT OVERBUILD] | `printable-document.tsx` is the single shared primitive; consume `layout='a4'`, don't rebuild |
| Duplicate accepted-plan/signed-acceptance viewer (in dental-visit) | [DO NOT OVERBUILD] | case-presentation owns it; dental-visit GAP-3 is consumption-wiring only |
| Standalone settings shell (in data-governance, dental-pmd, dental-clinical) | [DO NOT OVERBUILD] | dental-org owns the settings-panel registry; mount panels onto it |
| Consent-template backend/schema (in dental-clinical) | [DO NOT OVERBUILD] | Real CRUD already exists in dental-org `consentTemplates.ts`; clinical only wires the picker |
| Per-op Hurl response-shape pins for all 369 ops | DO NOT ADD | Brittle status quo that let 3 drifts through; use Schemathesis conformance instead |
| Central RBAC permission-registry engine | V2 DEFERRED | Immediate need is 8 declaration fixes + matrix-contradiction decision; full engine is overbuild today |
| Ceph AI auto-tracing / perio voice charting | DO NOT ADD | Intentional non-goals (local-first/offline, no-AI); gated by the §13 no-AI product decision |
| Drawn-signature image read-back (case-presentation) | V2 DEFERRED | Needs backend exposure of `TreatmentPlanApproval.signatureData` on a read path (out of FE-only scope) |

## 15. Recommended Next Step

Proceed to **`06-database-schema-audit.md`** next, per the ratified pipeline.

That prompt must pick up the cross-cutting schema items flagged in §11 (do not re-derive them — they are pre-scoped here):

1. **`pmd_document.visit_id` missing unique index / supersede race** — now reachable because dental-pmd's generation trigger sits on the visit-completion hot path (`services/api-ts/src/handlers/dental-pmd/generatePMD.ts` supersede-on-read; trigger at `services/api-ts/src/handlers/dental-visit/visits/updateDentalVisit.ts:168-185`). Evaluate a unique index / upsert. [NEEDS CONFIRMATION]
2. **`safety_floor_merged` text-typed boolean** on the PMD document schema — flag text→boolean; fold any migration into dental-pmd Batch C only.
3. **dental-perio nullable diagnosis columns** (`stage`/`grade`/`extent` text + `risk_factors` jsonb, migration `services/api-ts/src/generated/migrations/0101_complete_junta.sql`) — consistency check of nullable-column ↔ contract nullability (ties to the openapi-null-drop platform fix); no schema change implied.
4. **JSONB-PHI column inventory** — `services/api-ts/src/handlers/patient/repos/patient.schema.ts` (`emergencyContact`/`communicationPreferences`/`followUpNotes`) and `services/api-ts/src/handlers/person/repos/person.schema.ts` (`contactInfo`/`primaryAddress`/`consent`) — inventory to scope the §5 `core/logger.ts` recursive-redaction hardening against ground truth.
5. **Erasure anonymize-target coverage** — confirm `subjectPersonId`-keyed anonymize across the person/patient/dental-clinical/dental-imaging/storage facades is complete (note: `tenantId` is metadata-only; this is attribution-integrity, not wrong-patient deletion).

After `06-database-schema-audit.md`, the highest-leverage platform fix to schedule (per §12 order 1) is the shared validator-mounting `buildTestApp` test-harness + raw-handler lint guard — route it to a future specialized platform fix prompt, not `04-module-or-group-fix-tdd.md`, because it is cross-cutting across 9 modules rather than module-batchable. Module-batchable items (RBAC `["user"]→["admin"]` regen, optional-branchId lint guard, audit verb-parity test) belong in `04-module-or-group-fix-tdd.md` against their owning module slugs (`data-governance`, `dental-billing`, `dental-audit`).