<!-- oli-version: 1.1 | generated: 2026-05-24 | skill: oli-module-specs --all -->

# Module Specification: dental-scheduling

---
Spec Version: 1.0 | Last Updated: 2026-05-24
---

## 1. Module Overview
**Purpose:** Appointment lifecycle management. Supports create/reschedule/check-in/cancel with soft double-booking at create (warn) vs. hard block at reschedule (409). Check-in creates a linked Visit record that outlives the appointment.

**Users:** staff_full, staff_scheduling (scheduling ops); dentist_owner, dentist_associate (check-in, view); staff_scheduling (calendar view only)

**Related:** dental-org (working hours, assertBranchAccess), dental-visit (visit creation on check-in), dental-patient (patient lookup), notifs (appointment notifications), booking (slot generation — G-001)

---

## 2. Domain Terms

| Term | Definition |
|------|-----------|
| Appointment | Scheduled encounter slot for a patient+dentist at a branch |
| Check-in | Converting a scheduled appointment to an active Visit (BR-004) |
| Walk-in | Appointment created same-day bypassing slot availability (BR-SCH-002) |
| Double-booking | Two appointments for same dentist in same time window |
| Slot | Available booking window (generated by booking module pg-boss job) |

---

## 3. Workflows

| Workflow | Actor | Description | Priority |
|----------|-------|-------------|----------|
| WF-006 | staff_full, dentist_owner | Book appointment | P0 |
| WF-007 | staff_full, dentist | Check-in (BR-004) | P0 |
| WF-024 | all dental roles | Calendar/schedule view | P0 |
| WF-059 [INFERRED] | staff_full, dentist_owner | Cancel appointment | P1 |
| WF-060 [INFERRED] | staff_full, dentist_owner | Reschedule | P1 |
| WF-061 [INFERRED] | System (pg-boss) | Slot generation (G-001) | P2 |
| WF-080 [INFERRED] | System | Appointment confirmation notification | P2 |
| WF-081 [INFERRED] | System | 24h reminder notification | P2 |

---

## 4. Workflow Details
### WF-007: Check-in
Actor: staff_full | Steps: 1. Staff selects appointment 2. Click "Check-in" 3. System verifies BR-001 (no active visit for patient) 4. Creates Visit draft → active 5. Returns visitId | Exception: BR-001 violated → 409, prompt staff

### WF-006: Book Appointment
Actor: staff_full | Steps: 1. Select date/time/dentist 2. System checks for overlap 3. If overlap: warn DOUBLE_BOOKING but proceed (FR3.7) 4. Save | Reschedule: hard 409 on overlap

---

## 5. Business Rules

| Rule ID | Rule | Expected Behavior |
|---------|------|-------------------|
| BR-004 | Check-in creates visit; appointment delete ≠ visit delete | Visit persists after appointment cancel |
| BR-SCH-001 | All appointments scoped to branch | assertBranchAccess |
| BR-SCH-002 | Walk-in bypass slot availability | Skip working-hours check if walk_in=true |
| BR-SCH-003 | Cancellation requires reason | cancellation_reason NOT NULL |
| BR-SCH-004 | Appointments validate against branch working hours | 422 if outside hours (unless walk-in) |
| FR3.7 | Double-booking: soft-warn at create, hard-block at reschedule | Create → 201+warning; Reschedule → 409 |

---

## 6. Permissions

| Action | Allowed | Notes |
|--------|---------|-------|
| Create/reschedule | staff_full, staff_scheduling, dentist_owner, dentist_associate | — |
| Check-in | staff_full, dentist_owner, dentist_associate | Not staff_scheduling |
| Cancel | staff_full, dentist_owner | — |
| View calendar | all dental roles | — |

---

## 7. Data Requirements
**`dental_appointment`:** id, patient_id, branch_id, dentist_member_id, scheduled_at, duration_minutes, status (enum: scheduled/checked_in/completed/cancelled/no_show), walk_in (bool), cancellation_reason, cancelled_at, checked_in_at, visit_id (nullable, set on check-in)

**Wire vs storage field mapping (V-SCH-006/007):** The canonical wire contract (API_CONTRACTS.md) exposes `providerId` / `startAt` / `endAt` / `visitType`. The DB columns retain their historical names `dentist_member_id` / `scheduled_at` / `duration_minutes` / `service_type` to avoid a destructive migration; handlers translate via `appointment-wire.ts` (duration = endAt − startAt). `visitType` is a constrained enum — `checkup` | `treatment` | `emergency` | `recall`.

---

## 7b. Aggregate Boundaries
Appointment is an aggregate root. Visit referenced by visit_id (UUID only) after check-in — Visit is owned by dental-visit.

---

## 8. State Transitions
```
scheduled  ──► checked_in | cancelled | no_show
checked_in ──► completed   | cancelled | no_show
no_show    ──► completed   (reversible revert)
completed  ──► (terminal)
cancelled  ──► (terminal)
```
**FSM clarification (V-SCH-009):** `checked_in → cancelled`, `checked_in → no_show`, and `no_show → completed` are intentional transitions backed by `APPOINTMENT_TRANSITIONS` in `repos/dental-appointment.schema.ts`. `no_show → completed` is the documented "revert no-show" path (a patient who was marked no-show but actually attended). All other transitions are rejected with a 422/validation error.

No DB unique constraint on dentist+time (see FR3.7 — intentional).

**QueueItem state naming deviation (IDEAL-GAP-P2-011):** The IDEAL §3.3 standard names queue states as `waiting → with_provider → ready_for_checkout → checked_out`. The implementation uses `with_provider` and `ready_for_checkout` as the enum values (matching the standard for those two), but the overall QueueItem lifecycle in this codebase follows the appointment/visit flow rather than a separate queue entity. This is a deliberate structural deviation — no standalone `dental_queue_item` table exists; queue state is derived from appointment + visit status.

---

## 9. UI/UX Requirements
**Calendar view:** week/day grid, appointment blocks by dentist/room. **States:** Loading, Empty (no appointments), Appointment detail popover, Double-booking warning modal, Check-in confirmation.

---

## 10. API Expectations
POST /dental/appointments (FR3.7 double-booking), GET /dental/appointments (calendar query), PATCH /dental/appointments/:id (reschedule hard-block), POST /dental/appointments/:id/check-in (BR-004), DELETE /dental/appointments/:id (soft-cancel, reason required)

---

## 10b. Domain Events
**Published:** DE-010 AppointmentBooked, DE-011 AppointmentCancelled
**Consumed:** (none)

Per ADR-006 (domain-events-descope), domain events here are audit-log-only semantic markers — there is NO event bus. Producers satisfy them by writing the corresponding dental_audit_log row synchronously via logAuditEvent(); reactive consumers (e.g. notifs) are deferred to a future phase. No publisher/emit scaffolding is required.

**DE-001 VisitCheckedIn ownership (V-SCH-010):** VisitCheckedIn is OWNED by dental-visit — the visit lifecycle begins at check-in, and `createVisit` writes the audit-log marker. dental-scheduling's check-in handler only delegates visit creation; it does not own or emit VisitCheckedIn.

---

## 11. Acceptance Criteria
**AC-SCH-001:** Double-booking at create → 201 + DOUBLE_BOOKING warning in response body.
**AC-SCH-002:** Double-booking at reschedule → 409.
**AC-SCH-003:** Check-in with existing active visit for patient → 409.
**AC-SCH-004:** Cancel without reason → 422.
**AC-SCH-005:** Appointment cancelled → associated visit still accessible.

---

## 12. Test Expectations
Unit: FR3.7 (soft-warn vs hard-block), BR-SCH-004 working hours, BR-004 visit survival.
Integration: check-in creates visit in dental-visit module.

---

## 13. Edge Cases
- No-show appointment → status no_show; no visit created
- Walk-in outside working hours → allowed (BR-SCH-002)
- Appointment for patient at wrong branch → 403
- Slot generation job not implemented (G-001) → manual appointment creation only

---

## 14. Dependencies
**Internal:** dental-org (working hours), dental-visit (visit creation), dental-patient (patient lookup), notifs (confirmation/reminder), booking (slot generation — G-001 gap)
**External:** pg-boss (reminder job — WF-081)

---

## 15. Error Handling

| Scenario | HTTP | Code |
|----------|------|------|
| Reschedule conflict | 409 | RESCHEDULE_CONFLICT (V-SCH-001) |
| Check-in: active visit exists | 409 | CHECKIN_ACTIVE_VISIT (V-SCH-002) |
| Cancel without reason (query `reason` min:5/max:500) | 422 | REASON_REQUIRED |
| Outside working hours | 422 | OUTSIDE_WORKING_HOURS |
| endAt not after startAt | 400 | VALIDATION_ERROR (V-SCH-008) |
| visitType not in enum | 400 | VALIDATION_ERROR (V-SCH-007) |

---

## 16. Performance Expectations
Calendar load < 2s (1 month, 1 branch). Booking < 1s. Volume: ~50 appointments/day/branch.

---

## 17. Observability Hooks
dental-scheduling.booked (INFO), dental-scheduling.checked-in (INFO), dental-scheduling.cancelled (INFO, with reason), dental-scheduling.double-booking (WARN). No PII in log fields.

---

## 18. Feature Flags
| Flag | Default | Description |
|------|---------|-------------|
| dental_scheduling_slot_generation | false | Enable pg-boss slot generation (G-001) |
| dental_scheduling_sms_reminder | false | Enable 24h SMS reminder |

---

## 19. Vertical Slice Plan
SCH-S1: Create + view appointment | SCH-S2: Check-in → visit creation | SCH-S3: Cancel + reschedule | SCH-S4: Double-booking detection | SCH-S5: Notifications (confirmation + reminder) | SCH-S6: Slot generation (G-001)

---

## 20. AI Instructions
1. FR3.7 is intentional: create = soft-warn, reschedule = hard-block. Do not add DB unique constraint.
2. Check-in calls dental-visit's createVisit handler — this is a cross-module sync call (WF-089).
3. assertBranchAccess at top of every handler.
4. Follow ARCHITECTURE.md, CONTRIBUTING.md, VERTICAL_TDD.md.
