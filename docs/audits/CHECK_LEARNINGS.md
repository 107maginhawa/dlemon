---
oli-version: "1.0"
based-on:
  - docs/audits/CHECK_SUMMARY.md
  - docs/audits/codebase-map/.map-meta.json
  - .oli/config.json
last-modified: 2026-06-02
last-modified-by: oli-check
---

# OLI Check — Checker Self-Limitations

Rolling backlog of where **the checker itself** could not verify or lacks a rule (distinct
from project gaps, which live in CHECK_SUMMARY's matrix + gate). Paste back to harden the
skill. `◆` = new or recurring this run. These entries NEVER affect the GATE.

| category | observation | evidence_ref | suggested_improvement | first_seen | last_seen | times_seen |
|----------|-------------|--------------|-----------------------|------------|-----------|------------|
| freshness-trust | ◆ [evidenced] STALE-OVERLAP **DID trigger** this run (map@a3bfc9a vs HEAD@c26d37b — 2 stale modules `features`,`routes` + 70 unmapped code changes). `/oli-check` has no auto-refresh, so the aggregator had to run `oli-engine scan . --write` BY HAND before code dimensions could be graph-anchored. The refresh produced byte-identical artifacts (only `.map-meta.json` sha bumped) — i.e. a no-op rescan was mandatory just to clear the freshness flag. | Step-3.5 --check-fresh output; .map-meta.json (map@a3bfc9a→c26d37b, 0 artifact-hash deltas) | Have `/oli-check` auto-run `oli-codebase-map --incremental` (or `oli-engine scan --write`) when Step-3.5 detects STALE-OVERLAP, instead of degrading or requiring a manual pre-step. Also document that the map scopes frontend only — backend dimensions run map-blind by design. | 2026-05-31 | 2026-06-02 | 4 |
| engine-field-gap | ◆ [evidenced] `CODE_SPEC_TRACE.json` empty (0 matched ops, `spec_source: null`, phase 0ms) though OpenAPI ships ~239 ops — `spec_trace_optin: false`. Traceability fell back to the project `audit:trace` script; re-flagged TR-INFRA-001. Recurs every code-dimension run. | TRACE_REPORT §TR-INFRA-001; .map-meta.json provenance.spec_trace_optin | Enable `spec_trace_optin` + wire the OpenAPI doc into the engine spec-trace phase, or have Discovery flag the empty trace as a known opt-out (not stale). | 2026-05-31 | 2026-06-02 | 5 |
| engine-field-gap | ◆ [evidenced] `CODE_API_SURFACE.response_shape` empty for all frontend endpoints (blanket `is_phantom=true`), so traceability algorithm 5g (FE-field-phantom — the D2/D4 root-cause detector) could not run verified → routed to the `unverified` bucket per R1 (MEDIUM threshold). The most valuable FE↔BE field-contract check stays dark. | TRACE_REPORT §5g; .map-meta.json artifact_hashes.CODE_API_SURFACE | Teach the engine api-surface phase to populate `response_shape` from the TypeSpec/OpenAPI doc (140 dental paths ship schemas) so 5g runs instead of blanket-phantoming. | 2026-06-01 | 2026-06-02 | 2 |
| low-confidence-heuristic | [evidenced] Confidence L2 FE→BE edge density read **0.111** (2/18 data-hook consumers resolved → capped L2 at 6, Overall Test-Confidence 9→6) though FE is genuinely covered: 91 repo files use the raw-`fetch(${apiBaseUrl}/dental/…)` custom-hook pattern that engine `behavior.ts` does not resolve → empty `api_calls`. Mechanical cap, not a test hole. | CONFIDENCE_REPORT §5.5 (2026-06-02); CODE_COMPONENT_REGISTRY.json | Teach the behavior resolver BOTH the SDK hook-factory patterns AND the raw-`fetch(apiBaseUrl + path)` custom-hook pattern; until then treat the L2 edge-density cap as an engine-coverage signal, not a gate-failing defect. | 2026-05-31 | 2026-06-02 | 3 |
| dimension-coverage-gap | ◆ [evidenced] Compliance Step 9e (data-governance, incl. PHI at-rest) is flag-gated on `--regulated`/PRD `Regulated: YES`. This `--auto` run executed 9e (re-verified V-DG-001/002/003 RESOLVED), but the activation still depends on a flag the user didn't pass — the run-to-run instability that surfaced V-DG-001 P0 one run and PASS the next remains structurally possible. | COMPLIANCE_REPORT §9e; compliance.md Step 9e activation | Auto-activate `--regulated` from a project marker (PRD characteristic / `.planning/config.json regulated: true`) so the healthcare PHI-at-rest P0 cannot silently disappear when the flag is omitted. | 2026-06-01 | 2026-06-02 | 2 |
| config | ◆ [evidenced] Runtime boot-smoke orchestration assumed the web dev port `:3001` (and `:3002` for account); the dentalemon app actually serves on `:3003` (`vite.config server.port`). The dynamic agent had to rediscover the port at runtime, costing a boot cycle. Port assumptions in the verified-environment notes drift from `vite.config`. | RUNTIME_READINESS_REPORT (WEB_BOOT_SMOKE :3003); apps/dentalemon/vite.config | Read the bind port from `vite.config`/`package.json` at runtime-dimension start instead of a hardcoded assumption, so boot-smoke probes the right port first try. | 2026-06-02 | 2026-06-02 | 1 |
| engine-field-gap | [evidenced] (RESOLVED) Manual dental route registration kept dental endpoint PATHS out of compiled `openapi.json` (TR-DG-002 class). The manual-route→TypeSpec migration landed (OpenAPI 103→140 dental paths); traceability verified erasure ×4 + legal-hold ×2 now in the compiled contract → TR-DG-002 resolved. No recurrence. | TRACE_REPORT "Changes Since Last Run"; openapi.json 140 dental paths | None — resolved. Re-open only if new hand-mounted product routes reappear off the TypeSpec namespace. | 2026-05-31 | 2026-06-01 | 2 |
| new-stack-case | [evidenced] (RESOLVED) `UI_CONSISTENCY_SPEC.md` was absent → ui-consistency degraded to infer-from-code DRAFT mode (P3 cap). The spec now exists; enforcement Phase 1.6 ran ui-consistency (still DRAFT/P3-capped pending a non-inferred spec, but no longer skipped). | ENFORCEMENT_REPORT Phase 1.6; docs/product/UI_CONSISTENCY_SPEC.md present | Promote the inferred draft to a reviewed UI_CONSISTENCY_SPEC so the sub-check scores above the P3 cap. | 2026-05-31 | 2026-06-01 | 2 |
