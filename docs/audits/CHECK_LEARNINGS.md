---
oli-version: "1.0"
based-on:
  - docs/audits/CHECK_SUMMARY.md
  - docs/audits/codebase-map/.map-meta.json
  - .oli/config.json
last-modified: 2026-06-01
last-modified-by: oli-check
---

# OLI Check — Checker Self-Limitations

Rolling backlog of where **the checker itself** could not verify or lacks a rule (distinct
from project gaps, which live in CHECK_SUMMARY's matrix + gate). Paste back to harden the
skill. `◆` = new or recurring this run. These entries NEVER affect the GATE.

| category | observation | evidence_ref | suggested_improvement | first_seen | last_seen | times_seen |
|----------|-------------|--------------|-----------------------|------------|-----------|------------|
| dimension-coverage-gap | ◆ [evidenced] Compliance `--regulated` Step 9e (data-governance, incl. PHI at-rest encryption) is the gate-deciding check this run — it surfaced **V-DG-001 P0** (plaintext PHI vs DATA_GOVERNANCE §1). The prior 3 runs reported Compliance PASS / 0 P0, because 9e only activates under `--regulated` (or PRD `Regulated: YES`). Same code, opposite verdict, depending on a flag → run-to-run instability on a healthcare project's most important check. | CHECK_SUMMARY §4 V-DG-001; COMPLIANCE_REPORT §9e; compliance.md Step 9e activation | Auto-activate `--regulated` from a project marker (PRD characteristic / `.planning/config.json regulated: true`) so the PHI-at-rest P0 cannot silently disappear when the flag is omitted. | 2026-06-01 | 2026-06-01 | 1 |
| engine-field-gap | ◆ [evidenced] `CODE_API_SURFACE.response_shape` is empty for **all 43 frontend endpoints** (blanket `is_phantom=true`, api_calls carry no field-access data), so traceability algorithm 5g (FE-field-phantom — the D2/D4 root-cause detector) could not run verified; routed to the `unverified` bucket per R1 (confidence_threshold MEDIUM). The most valuable FE↔BE field-contract check is effectively dark. | TRACE_REPORT §5g; .map-meta.json artifact_hashes.CODE_API_SURFACE | Teach the engine api-surface phase to populate `response_shape` from the TypeSpec/OpenAPI doc (the project ships 140 dental paths with schemas) so 5g can run instead of blanket-phantoming. | 2026-06-01 | 2026-06-01 | 1 |
| engine-field-gap | ◆ [evidenced] `CODE_SPEC_TRACE.json` is empty (0 matched ops, `spec_source: null`, phase 0ms) though the OpenAPI doc ships ~239 ops — `spec_trace_optin: false` in provenance. Traceability fell back to the project `audit:trace` script. Re-flagged as TR-INFRA-001 again this run. Recurs. | TRACE_REPORT §TR-INFRA-001; .map-meta.json provenance.spec_trace_optin | Enable `spec_trace_optin` + wire the OpenAPI doc into the engine spec-trace phase, or have Discovery flag the empty trace as a known opt-out (not stale). | 2026-05-31 | 2026-06-01 | 4 |
| engine-field-gap | [evidenced] (RESOLVED this run) Manual dental route registration kept dental endpoint PATHS out of compiled `openapi.json`, so traceability flagged each dental group as a wire-contract P1 (TR-DG-002 class). **The manual-route→TypeSpec migration landed on this branch** (OpenAPI 103→140 dental paths); traceability this run verified erasure ×4 + legal-hold ×2 paths now in the compiled contract and **resolved TR-DG-002**. No recurrence — the divergence class is closed. | TRACE_REPORT "Changes Since Last Run"; openapi.json 140 dental paths | None — resolved. (Keep as a record; re-open only if new hand-mounted product routes reappear off the TypeSpec namespace.) | 2026-05-31 | 2026-06-01 | 2 |
| low-confidence-heuristic | [evidenced] Confidence L2 FE→BE edge density read 0.11 (capped L2 at 6) though FE is genuinely covered — the engine `behavior.ts` resolver does not resolve `@monobase/sdk-ts` `*Options()`/`/react/hooks` factories, so real data-hook consumers get empty `api_calls`. (Confidence dimension NOT run this run — carried, not re-evidenced.) | CONFIDENCE_REPORT §5.5 (prior run) | Teach the behavior resolver the SDK `*Options()`/hook factory patterns so edge density reflects real FE→BE wiring. | 2026-05-31 | 2026-05-31 | 2 |
| new-stack-case | [evidenced] (RESOLVED this run) `UI_CONSISTENCY_SPEC.md` was absent → ui-consistency degraded to infer-from-code DRAFT mode (P3 cap). The spec now exists; enforcement Phase 1.6 ran ui-consistency this run (still DRAFT/P3-capped pending a non-inferred spec, but no longer skipped). | ENFORCEMENT_REPORT Phase 1.6; docs/product/UI_CONSISTENCY_SPEC.md present | Promote the inferred draft to a reviewed UI_CONSISTENCY_SPEC so the sub-check can score above the P3 cap. | 2026-05-31 | 2026-06-01 | 2 |
| freshness-trust | [evidenced] (Did NOT trigger this run) Prior runs began STALE-OVERLAP and needed a manual `oli-engine scan` before code dimensions. This run the map was FRESH for its scope (`apps/dentalemon/src`) with 0 in-scope changes despite 8 commits ahead — no manual refresh needed. The known limitation (no auto-refresh on STALE-OVERLAP) is unchanged but was not exercised. | --check-fresh; .map-meta.json (map@a3bfc9a vs HEAD@ece7f89c, 0 in-scope diff) | Still: have `/oli-check` auto-run `oli-codebase-map --incremental` when Step-3.5 detects STALE-OVERLAP. (Also: the map scopes frontend only — backend dimensions run map-blind by design; document this so backend isn't assumed graph-anchored.) | 2026-05-31 | 2026-06-01 | 3 |
| toolchain-gated | [evidenced] Runtime `--live` + Seed-Coherence depend on a booted app (:7213) and were SKIP'd on prior runs (API down). NOT selected this run (`--enforcement --traceability --compliance` only). Carried. Also this run: enforcement confirmed migrated-route wiring via static analysis only (registry imports + auth guards + audit emission), respecting the project rule against `bun test <path>` — runtime route-registration tests not executed. | ENFORCEMENT_REPORT scope note; project test-DB guard | Document a one-liner pre-step to boot the stack so the live tier isn't silently skipped; for route-registration confidence, rely on the full `bun run test` suite (2957/0) rather than per-path runs. | 2026-05-31 | 2026-06-01 | 3 |
