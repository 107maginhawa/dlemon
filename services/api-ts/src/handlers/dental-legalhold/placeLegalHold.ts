/** Codegen entrypoint for operationId `placeLegalHold` (re-exports the impl;
 * named *Handler to avoid colliding with the placeLegalHold service fn). */
export { placeLegalHoldHandler as placeLegalHold } from './placeLegalHoldHandler';
