services:
  postgres:
    image: postgres:16-alpine
    container_name: monobase-test-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: monobase
    ports:
      # Host port defaults to 5432 (CI / clean machines). On a machine that also
      # runs a local postgres on 5432 (e.g. homebrew), that local pg shadows this
      # container for host connections and routes the API to the wrong DB — set
      # DLEMON_PG_PORT=5433 in services/api-ts/.env (+ matching DATABASE_URL) to
      # give dlemon its own host port. Compose reads that .env automatically.
      - "${DLEMON_PG_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d monobase"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - test-network
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-test-db.sql:/docker-entrypoint-initdb.d/init-test-db.sql:ro

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: monobase-test-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_REGION_NAME: us-east-1
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - test-network
    volumes:
      - minio_data:/data

  mailpit:
    image: axllent/mailpit:v1.21.0
    container_name: monobase-test-mailpit
    ports:
      - "1025:1025"  # SMTP server
      - "8025:8025"  # Web UI
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8025/api/v1/info"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - test-network

  stripe-mock:
    image: stripe/stripe-mock:v0.193.0
    container_name: monobase-test-stripe-mock
    ports:
      - "12111:12111"  # HTTP
    networks:
      - test-network

  # Optional, takes too much time to start, take resources
  # coturn:
  #   image: coturn/coturn:latest
  #   container_name: monobase-test-coturn
  #   command:
  #     - "-n"
  #     - "--log-file=stdout"
  #     - "--listening-port=3478"
  #     - "--fingerprint"
  #     - "--lt-cred-mech"
  #     - "--user=monobase:monobase123"
  #     - "--realm=monobase.local"
  #     - "--no-dtls"
  #     - "--no-tls"
  #   environment:
  #     DETECT_EXTERNAL_IP: "yes"
  #     DETECT_RELAY_IP: "yes"
  #   ports:
  #     - "3478:3478/tcp"      # STUN/TURN TCP
  #     - "3478:3478/udp"      # STUN/TURN UDP
  #     - "5349:5349/tcp"      # STUN/TURN TLS (disabled but reserved)
  #     - "49152-65535:49152-65535/udp"  # UDP relay port range
  #   healthcheck:
  #     test: ["CMD", "turnutils_uclient", "-v", "-t", "1", "-T", "1", "127.0.0.1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 5s
  #   networks:
  #     - test-network

networks:
  test-network:
    driver: bridge
    name: monobase-test-network

volumes:
  postgres_data:
  minio_data:
